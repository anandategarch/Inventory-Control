# Nixpacks configuration for Railway
# Uses Bun for faster builds

[phases.setup]
nixPkgs = ["bun"]
envs = ["DATABASE_URL=file:./db/custom.db"]

[phases.install]
cmds = ["bun install"]

[phases.build]
cmds = [
  "mkdir -p db",
  "bun run db:push",
  "bun run build"
]

[start]
cmd = "bun run start"
