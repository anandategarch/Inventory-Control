# Nixpacks configuration for Railway / Render / Fly.io
# Uses Bun for faster builds

[phases.setup]
nixPkgs = ["bun"]

[phases.install]
cmds = ["bun install"]

[phases.build]
cmds = [
  "bunx prisma generate",
  "bun run build"
]

[start]
cmd = "bun run start"
