---
Task ID: SPLIT-D
Agent: full-stack-developer
Task: Refactor murni (TIDAK ada fitur baru): pecah 3 file lib besar — `src/lib/settings.ts` (600 baris), `src/lib/drive-import.ts` (595), `src/lib/ingestion/process-ingestion.ts` (565) — menjadi modul kecil per tanggung jawab, dengan SEMUA export lama tetap bisa diimport dari path lama yang sama (file lama dihapus, folder bernama sama + `index.ts` barrel menggantikan — resolusi `@/lib/settings` / `@/lib/drive-import` / `./process-ingestion` tidak berubah bagi caller). Mengikuti pola split sukses REFACTOR-1-a (aggregation-cache / growth-drivers: folder + barrel named re-export eksplisit, file lama dihapus) dan SPLIT-GOD-FILE b4b37d2.

Work Log:
- Baca `tail -150 worklog.md` (pola SPLIT-GOD-FILE commit b4b37d2), `agent-ctx/REFACTOR-1-a-fullstack.md`, `agent-ctx/SPLIT-COMPONENTS-component-splitter.md` → konvensi: folder + `index.ts` barrel dengan named re-export EKSPLISIT (tanpa `export *`), file lama dihapus, komentar FIX/BUG dipertahankan verbatim, type-level adaptation boleh selama runtime identik.
- Audit caller SEBELUM split: `rg -c "from '@/lib/settings'"` = 18 file (16 src + 2 tests, termasuk `vi.mock('@/lib/settings')` di tests/queries/health-ranking.test.ts:44 dan real-import `invalidateSettingsCache` di tests/app/outlet-items-resolve-period.test.ts:16); `from '@/lib/drive-import'` = 1 file (src/app/api/import-drive/route.ts → importFromDriveUrl); `./process-ingestion` hanya direferensikan `src/lib/ingestion/index.ts:51` (`export * from './process-ingestion'` — file ini READ-ONLY, tidak disentuh; resolusi otomatis beralih dari file ke folder index.ts).
- Konfirmasi tidak ada `vi.mock` terhadap `@/lib/drive-import` / `@/lib/ingestion`; tidak ada import subpath (`@/lib/settings/...`) di mana pun; tidak ada referensi path di next.config/vitest.config/tsconfig/eslint.config.
- Baseline lint 3 file lama: hanya 2 warning pre-existing di drive-import.ts:196 (`entryBlocks` unused) — dipertahankan (kode bergerak verbatim; bug dead-code TIDAK diperbaiki, sesuai batasan).
- SPLIT settings.ts → `src/lib/settings/`: `definitions.ts` (katalog SETTING_DEFINITIONS + interface SettingDefinition), `store.ts` (state cache `_settingsCache`/`_cacheLoadedAt`/`CACHE_TTL_MS`/`_defaultsEnsured` + loadSettingsFromDB + ensureDefaultSettings + getAllSettings + getSetting(+Number/Bool) + invalidateSettingsCache — singleton state didefinisikan SEKALI di sini), `runtime-thresholds.ts` (interface RuntimeThresholds + getRuntimeThresholds), `index.ts` barrel (10 export lama, named re-export eksplisit). Import graph linear: definitions ← store ← runtime-thresholds; tanpa siklus.
- SPLIT drive-import.ts → `src/lib/drive-import/`: `types.ts` (DriveFile/DriveImportResult/DriveIdType), `url-parse.ts` (extractDriveId), `http-safety.ts` (FETCH_TIMEOUT_MS/FOLDER_LIST_TIMEOUT_MS/MAX_DOWNLOAD_BYTES/readTextCapped/pipelineWithByteCap — konstanta didefinisikan sekali; internal, tidak ikut barrel), `folder-scan.ts` (listDriveFolderFiles), `download-file.ts` (downloadDriveFile), `download-sheets.ts` (downloadGoogleSheetsAsCsv + getSheetsTitle — getSheetsTitle kini di-export di level modul untuk dipakai import-url.ts, TETAPI tidak di-re-export barrel sehingga API publik `@/lib/drive-import` tetap persis 8 export lama), `import-url.ts` (importFromDriveUrl), `index.ts` barrel.
- SPLIT process-ingestion.ts → `src/lib/ingestion/process-ingestion/`: `resolve-files.ts` (resolveInputFiles — discriminated union `{ok:true,files}|{ok:false,errorResult}` menggantikan 4 early-return; caller: `if (!resolved.ok) return [resolved.errorResult]` — perilaku identik), `read-rows.ts` (readAllRows + tipe IngestRawRow), `prepare-rows.ts` (PASS 1 CPU-only; seenKeys + allIssues dilewat by-reference — identik closure lama; totalRows lokal karena tak dipakai luar PASS 1), `ingest-transaction.ts` (runIngestTransaction — blok `db.$transaction` utuh verbatim; naturalKeys/weekDbMap/batchRecords lokal di sini karena hanya dipakai dalam tx; `skippedDuplicates` di-return agar orchestrator tetap mencatat log AUDIT-BUG-3 di titik yang sama; raced-branch tetap return `skippedErrors: 0` hardcoded seperti aslinya), `orchestrator.ts` (processIngestion: loop per-file + lock + manual-rename + hash precheck + stale-stub purge + STEP 3 pre-cache + post-tx cache invalidation + catch/finally), `index.ts` barrel (hanya `processIngestion` — satu-satunya export lama).
- Hapus 3 file lama (`git rm`-equivalent via `rm`; Main yang commit). `src/lib/ingestion/index.ts` dan `src/lib/ingestion/process-rows-for-import.ts` TIDAK disentuh (READ-ONLY) — hanya di-import dari modul baru.
- Verifikasi import caller SESUDAH split: `from '@/lib/settings'` kini 20 file — 18 file lama TIDAK berubah + 2 file BARU milik agent paralel (src/app/api/export-report/services/data-fetcher/{context,setup}.ts) yang baru dibuat sesi ini dan resolve lewat barrel saya (tsc lolos). `from '@/lib/drive-import'` tetap 1 file. Nol perubahan pada file caller.
- `bunx tsc --noEmit --incremental false`: 0 error di domain SPLIT-D (berulang kali sepanjang sesi).
- `bunx eslint src/lib/settings/ src/lib/drive-import/ src/lib/ingestion/process-ingestion/`: 0 error, 2 warning — persis 2 warning pre-existing `entryBlocks` yang berpindah 1:1 dari drive-import.ts:196 → folder-scan.ts:48 (tidak ada warning baru).
- `bun run lint` (full): 0 error, 389 warning — seluruhnya baseline pre-existing di tests/ + file agent paralel.
- `bunx vitest run` (FULL suite, zero test edits): **31 file, 512/512 PASS** — termasuk 4 file yang menyentuh domain: tests/queries/health-ranking.test.ts (6 — `vi.mock('@/lib/settings')` tetap intercept lewat barrel), tests/app/outlet-items-resolve-period.test.ts (8 — real invalidateSettingsCache lewat barrel), tests/queries/rule-evaluation.test.ts (16), tests/lib/metrics/historical.test.ts (23). Tidak ada test yang meng-exercise processIngestion langsung (rg: none).
- Catatan error luar domain yang terlihat saat verifikasi final (paralel, transient, TIDAK diperbaiki sesuai instruksi): `src/app/api/export-report/services/data-fetcher/query-batch.ts(244,47) TS2345` (agent split export-report, masih in-flight); `src/components/dashboard/narrative/OutletPriorityPanel/index.tsx(181,13) TS2322 'kelompuk' vs 'kelompok'` — muncul sekali lalu hilang beberapa detik kemudian (agent component-splitter sedang mengedit).

Stage Summary:
- Peta file baru (baris) — pola KONSISTEN ketiganya: file lama DIHAPUS → folder bernama sama + `index.ts` barrel named re-export eksplisit:
  - `src/lib/settings/` (600 → 649 baris di 4 file): definitions.ts 363, store.ts 138, runtime-thresholds.ts 120, index.ts 28. File lama: DIHAPUS.
  - `src/lib/drive-import/` (595 → 674 baris di 8 file): types.ts 25, url-parse.ts 40, http-safety.ts 95, folder-scan.ts 92, download-file.ts 115, download-sheets.ts 151, import-url.ts 122, index.ts 34. File lama: DIHAPUS.
  - `src/lib/ingestion/process-ingestion/` (565 → 769 baris di 6 file): index.ts 15, orchestrator.ts 228, resolve-files.ts 80, read-rows.ts 35, prepare-rows.ts 88, ingest-transaction.ts 323. File lama: DIHAPUS.
  - (Pertumbuhan total baris = header modul + import statement + arg-threading mekanis; isi logika verbatim.)
- Dua file >250 baris, disengaja + terdokumentasi: `definitions.ts` (363 — katalog data murni satu tanggung jawab; memecah array per kategori akan mengubah urutan tampil di UI Settings = perubahan perilaku) dan `ingest-transaction.ts` (323 — inti transaksi atomik BUG2-INGEST-1 dengan ~11 state mutable yang saling terkait: naturalKeys/weekDbMap/batchRecords/totalInserted/skippedDuplicates/allIssues; memecahnya butuh ctx mutable besar = risiko drift perilaku; preseden repo meninggalkan unit koheren besar utuh — pdf-primitives 663 baris di SPLIT-GOD-FILE).
- Bukti caller tidak berubah: 18/18 file importer `@/lib/settings` lama tetap import path sama (plus 2 file baru agent paralel juga resolve via barrel); 1/1 importer `@/lib/drive-import`; `@/lib/ingestion` barrel (`export * from './process-ingestion'`) tak disentuh — resolusi bergeser ke folder secara transparan; `vi.mock('@/lib/settings')` tetap bekerja (health-ranking 6/6 pass).
- Verifikasi domain: tsc 0 error (domain), eslint domain 0 error + 2 warning inherited 1:1, vitest FULL 512/512 (31 file) tanpa satu pun edit test. Error luar domain yang terlihat: 1–2 transient di file agent paralel (export-report data-fetcher, OutletPriorityPanel) — dicatat, tidak disentuh.
- Catatan risiko/bug (TIDAK diperbaiki): (1) `entryBlocks` dead code (folder-scan.ts:48, ex-drive-import.ts:196) — variabel diisi tapi tak pernah dibaca, kemungkinan sisa refactor parser lama; (2) `skippedErrors` yang di-return transaksi tidak pernah dipakai caller (dead return field, behavior dijaga verbatim); (3) komentar di process-rows-for-import.ts:69 dan scripts/fix-null-akun-duplicates.ts:49 masih menyebut "process-ingestion.ts" sebagai nama file — kini folder, komentar saja, tanpa efek runtime; (4) tidak ada test yang meng-cover processIngestion end-to-end (pre-existing gap) sehingga jaminan perilaku pipeline ingestion bertumpu pada tsc + code review verbatim + pola yang sama dengan yang sudah dibuktikan REFACTOR-1-a.
- Yang TIDAK dilakukan: tidak commit/push (Main yang commit), tidak menulis worklog.md (laporan ini penggantinya), tidak menjalankan dev/build/db:*, tidak mengubah file di luar domain, tidak menambah dependency, tidak memperbaiki bug.
