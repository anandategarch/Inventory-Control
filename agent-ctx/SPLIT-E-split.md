---
Task ID: SPLIT-E
Agent: full-stack-developer
Task: Pure code-motion split of 2 query god files — `src/lib/queries/items/top-items/by-other-metric.ts` (645 lines, 10 exports) and `src/lib/queries/outlets/change-analysis.ts` (501 lines, 13 exports) — into small per-concern modules (ideal <250 lines/file) with the old paths kept as thin re-export barrels. Zero SQL/logic/order/result changes; `tests/queries/change-analysis.test.ts` must stay green UNMODIFIED.

Work Log:
- Read `worklog.md` tail (150 lines) for project context + the SPLIT-GOD-FILE (b4b37d2) / REFACTOR-1-a barrel patterns; read `agent-ctx/REFACTOR-1-a-fullstack.md` + `agent-ctx/OPTIMIZE-ENGINE-engine-optimizer.md` for conventions.
- Read both target files fully; enumerated public API (10 + 13 exports) and private symbols (CATEGORY_COLS, directionFromSums, WideCategoryRow → only used by queryTopItemsByAllCategories; OutletScanRow → outlet ranking; ItemScanRow → item attribution).
- Caller census (rg): `by-other-metric` is imported ONLY via `src/lib/queries/items/top-items/index.ts` (`export * from './by-other-metric'`, then consumed through `@/lib/queries/items/top-items` by tests/queries/top-items.test.ts, outlet-items services, export-report data-fetcher). `change-analysis` is imported ONLY by `src/app/api/change-analysis/route.ts`, `src/app/api/change-analysis/items/route.ts`, `tests/queries/change-analysis.test.ts` — all via `@/lib/queries/outlets/change-analysis`. No vi.mock of either module path (tests mock `@/lib/db` + `@/lib/logger` only) → sibling-file barrel strategy is safe. Chose SIBLING FILES (`by-other-metric-*.ts`, `change-analysis-*.ts`) + old file as thin barrel: keeps the exact old file paths (no delete), keeps relative import depth identical (`../../shared`, `./types`, `../shared`), consistent with the flat `outlets/` folder and the `by-deviasi-rank.ts` sibling convention in `top-items/`.
- Baseline before touching anything: `bunx vitest run tests/queries/change-analysis.test.ts tests/queries/top-items.test.ts` → 17 + 12 tests PASS.
- Split `by-other-metric.ts` into 5 sibling modules via `sed -n 'X,Yp'` verbatim extraction (byte-identical code motion, no retyping): `by-other-metric-nominal-devbom.ts` (queryTopItemsByNominal + queryTopItemsByDevBom, orig lines 33-105), `by-other-metric-all-categories.ts` (queryTopItemsByAllCategories + TopItemsCategoryRow + TopItemsAllCategories + private CATEGORY_COLS/directionFromSums/WideCategoryRow, orig 107-253), `by-other-metric-category-averages.ts` (queryHistoricalCategoryAvg + queryAreaCategoryAvg + AreaCategoryAvg, orig 255-398), `by-other-metric-item-consistency.ts` (queryItemConsistency, orig 400-456), `by-other-metric-pareto.ts` (queryParetoByDevBom + the historical "Network Item Risk" design-note banner kept verbatim, orig 458-645).
- Rewrote `by-other-metric.ts` (645 → 53 lines) as thin barrel: original header lines 1-28 kept byte-identical (sed) + SPLIT-E module map + explicit named re-exports (`export {}` / `export type {}`) of exactly the old 10 public symbols. `top-items/index.ts`'s `export * from './by-other-metric'` untouched and still resolves.
- Split `change-analysis.ts` into 4 sibling modules (same sed technique): `change-analysis-stats.ts` (pure computeChangeStats + classifyChange + ChangeSeriesPoint/ChangeStats/ChangeThresholds/ChangeStatus, orig 48-184; imports only calcGrowthAbs), `change-analysis-outlet-ranking.ts` (queryOutletChangeAnalysis + OutletChangeRow + ChangeAnalysisResult + private OutletScanRow, orig 186-300), `change-analysis-item-attribution.ts` (queryOutletChangeItems + ItemChangeRow + ChangeAnalysisItemsResult + private ItemScanRow, orig 302-461), `change-analysis-context.ts` (resolveChangeAnalysisContext, orig 463-501; imports only db).
- Rewrote `change-analysis.ts` (501 → 71 lines) as thin barrel: original semantics header lines 1-43 kept byte-identical + module map + explicit re-exports of exactly the old 13 public symbols.
- Import wiring per sibling: `../shared` / `../../shared`, `./types`, `@/lib/db`, `@/lib/metrics/growth`, `@prisma/client` — unchanged module sources; cross-sibling type imports (`./change-analysis-stats`, `./change-analysis-outlet-ranking`) replace what was intra-file before. No circular imports (siblings never import the barrel).
- PURITY PROOF (byte-level): `git show HEAD:` of both originals vs the new files — `diff` of each of the 9 moved bodies against its original line range = IDENTICAL for all 9; barrel headers (1-28 / 1-43) = IDENTICAL. Zero characters of moved SQL/logic/comments changed.
- API-surface proof: grep-compare of `^export` symbols, original vs union of new modules: 10/10 (by-other-metric) and 13/13 (change-analysis) — no lost/added exports. Runtime proof: `bun -e` dynamic-import of both barrels → all 7 + 5 function exports resolve as functions (NONE missing).
- GATES: `bunx tsc --noEmit --incremental false` → 0 errors WHOLE PROJECT. `bunx vitest run tests/queries/change-analysis.test.ts` → 17/17 PASS (test file untouched, READ-ONLY respected). Full suite `bunx vitest run` → 512/512 PASS (31 files) — matches the worklog's known-good baseline. `bun run lint` → 0 errors, 400 warnings; ZERO findings in my 11 domain files (warnings are the pre-existing baseline in tests/ + routes + other agents' areas). `rg -l "by-other-metric" tests/` → no matches (no other test file touches it; top-items.test.ts covers the functions via the parent barrel and passes).
- `git status` footprint: only `M` on the 2 old paths + 9 new untracked sibling files, all inside my domain. Other agents' parallel changes (scripts/audit/migration/, ItemTrendTab/, TopGrowthCard/, src/lib/settings/, etc.) untouched. No git add/commit/push/stash/reset performed; worklog.md not written (this file is the report).

Stage Summary:
- FILE MAP — by-other-metric (was 1 file / 645 lines → 6 files / 732 lines, largest 204):
    src/lib/queries/items/top-items/by-other-metric.ts                    53  (thin barrel, old path preserved)
    src/lib/queries/items/top-items/by-other-metric-nominal-devbom.ts     87  (top-N by nominal + by Dev/BOM)
    src/lib/queries/items/top-items/by-other-metric-all-categories.ts    160  (single-scan 4-category ranking + private helpers)
    src/lib/queries/items/top-items/by-other-metric-category-averages.ts 159  (historical avg + area avg benchmarks)
    src/lib/queries/items/top-items/by-other-metric-item-consistency.ts   69  (SYSTEMIC/WIDESPREAD/ISOLATED tiers)
    src/lib/queries/items/top-items/by-other-metric-pareto.ts            204  (Pareto 80/20 by |Dev/BOM|)
- FILE MAP — change-analysis (was 1 file / 501 lines → 5 files / 591 lines, largest 182):
    src/lib/queries/outlets/change-analysis.ts                           71  (thin barrel, old path preserved)
    src/lib/queries/outlets/change-analysis-stats.ts                    150  (pure stats + classify, test-facing)
    src/lib/queries/outlets/change-analysis-outlet-ranking.ts           136  (outlet-level ranking query)
    src/lib/queries/outlets/change-analysis-item-attribution.ts         182  (item attribution query)
    src/lib/queries/outlets/change-analysis-context.ts                   52  (week-fallback + monthKey resolution)
- Combined: 1.146 → 1.323 lines across 11 files (+177 = provenance headers + module maps + per-file imports only; 0 logic lines changed). All files < 250 lines.
- CALLERS UNCHANGED (rg proof): `@/lib/queries/outlets/change-analysis` still imported verbatim by src/app/api/change-analysis/route.ts:24, src/app/api/change-analysis/items/route.ts:22, tests/queries/change-analysis.test.ts:28; `./by-other-metric` still re-exported verbatim by src/lib/queries/items/top-items/index.ts:26. No caller file was modified.
- No duplicated constants: CATEGORY_COLS/directionFromSums/WideCategoryRow defined once (all-categories sibling, non-exported); OutletScanRow/ItemScanRow once each; DIRECTION_FROM_SUM_SQL still defined once in queries/shared.ts; no cache keys existed in these files (routes own them).
- VERIFICATION: tsc 0 errors (whole repo) · vitest change-analysis 17/17 (unmodified guard test) · vitest full 512/512 · eslint 0 errors / 0 warnings in domain files · 9/9 moved bodies + 2 barrel headers byte-identical vs HEAD · export parity 10/10 + 13/13 · runtime barrel import smoke OK.
- RISKS / BUGS NOTED (NOT fixed, per task rules):
    1. Dead doc: the "Network Item Risk" banner (orig by-other-metric.ts lines 458-476) describes a per-item network risk scoring function that does not exist anywhere in the codebase — leftover design note; kept verbatim in by-other-metric-pareto.ts with an explanatory line in that file's header.
    2. Pre-existing behavior worth a look someday: queryHistoricalCategoryAvg pins `ir."weekLabel" = historicalPeriods[0].weekLabel` for ALL historical months (orig line 291) — historical periods pinned to a different week than the first entry's are silently excluded from the baseline.
    3. Cosmetic doc drift preserved verbatim: by-other-metric barrel header still says "Five query functions" while listing 6 (queryAreaCategoryAvg was added later by REFINE-1).
    4. Project-wide lint warnings (400) are the pre-existing baseline concentrated in tests/ + API routes (non-null assertions, unused `tx` mock params) — none introduced by this split.
    5. Six parallel agents' in-flight edits are present in the working tree (git status shows their new folders); this split's verification ran green on top of them, but their domains are theirs to gate.
