---
Task ID: VERIFY-CORE
Agent: full-stack-developer
Task: Audit kecocokan implementasi vs MASTER_CONTEXT.md (single source of truth) untuk domain **metrik inti**: deviasi/residual/direction/z-score/growth. Repo `/home/z/audit-inventory` saja; DB/dev/build tidak dijalankan (verifikasi via kode + test unit). File yang boleh diubah: `src/lib/metrics*`, `src/lib/queries/historical*.ts`, `src/lib/queries/dashboard*.ts`, `src/lib/excel.ts` + `src/lib/csv-parser.ts` (mapping ingest), `src/lib/ingestion/process-rows-for-import.ts`.

Work Log:
- Baca MASTER_CONTEXT.md §Domain Problem (baris 26-47), §Anomaly Engine (281-315), §Historical Z-Score (183/186/325), settings/threshold claim (411), rules table (302-311), PAKET E compliance (387-406).
- Baca seluruh file domain inti: `src/lib/metrics/{definitions,deviation,growth,historical,sample-stats,sales,index}.ts`, `src/lib/queries/{historical,historical-baseline,dashboard,rule-evaluation,health-ranking,item-trend,pareto/compute}.ts`, `src/engine/{transform,validator}.ts`, `src/lib/ingestion/process-rows-for-import.ts`, `src/lib/{excel,csv-parser,zScoreHelpers,format}.ts`, `src/lib/settings/{definitions,runtime-thresholds}.ts`, `src/config/{thresholds,rules.yaml}`, `prisma/schema.prisma`.
- Baca (READ-ONLY) konsumen FE: `HistoricalZScoreCard.tsx`, `zScoreHelpers.ts`, `ZScoreCell.tsx`, `DeviationBreakdownChart.tsx`, `anomaly-flags.tsx`, `PeerTableRow.tsx` + pipeline z-score: `post-process-historical.ts`, `fetch-records.ts`, `post-process-growth.ts`, `exec-summary.ts`, `growth-drivers/*`, `item-history/route.ts`, `scripts/migrate-direction.ts`.
- Grep sistematis: `qtyCom -|qtyBom -|COM - BOM` (0 perhitungan deviasi di kode), `nominalLossSurplus [<>] 0` (semua gating LOSS), `nominalDeviasi [<>] 0` di queries (hanya `ABS(...) > 0` — tidak dipakai untuk arah), `RESIDUAL_LOSS_*_PCT` (semua sumber default konsisten), `zScore` (5 jalur komputasi + FE), `calcGrowth/computeNominalDeviationGrowth`.
- Jalankan test: full suite `bunx vitest run` (512/512 pass) + subset domain 2× (pra & pasca fix).
- Fix kosmetik 2 file domain (lihat bawah), lalu verifikasi pasca-fix.

TABEL VERIFIKASI PER KLAIM MASTER:

| # | Klaim Master (verbatim) | Lokasi Implementasi | Status | Bukti |
|---|---|---|---|---|
| 1 | "QTY Deviation (= COM − BOM)" | `excel.ts:30` (`'qty deviasi'→qtyDeviasi`), `csv-parser.ts:31-44` (header via `normalizeHeader` shared), `engine/transform.ts:201` (`toNum` verbatim), `process-rows-for-import.ts:328` (disimpan verbatim) | **MATCH** | Kolom QTY DEVIASI dibaca verbatim dari sumber — TIDAK ADA kode di pipeline yang menghitung deviasi dari COM/BOM (grep `qtyCom -` / `qtyBom -` / `COM - BOM` → 0 lokasi; `qtyBom - qtyBom` hanya sort comparator). Artinya tidak ada risiko tanda terbalik (BOM−COM) yang diintroduksi kode. Tanda konsisten dgn master: BOM/COM konsumsi bernilai negatif (`validator.ts:150-158` warning BOM_POSITIVE; `deviation.ts:28-33` "LOSS: qtyDeviasi < 0, nominalDeviasi < 0 — verified from production data") → COM−BOM < 0 ⟺ \|COM\|>\|BOM\| (aktual > SOC) ⟺ LOSS ⟺ `nominalLossSurplus < 0` = master "actual > SOC → loss". Cross-check identitas NET ada (`NET_DEVIATION_MISMATCH`: `validator.ts:198-221`, `transform.ts:291-312`: expectedNet = qtyDeviasi − explainedMag×sign(qtyDeviasi)). Catatan: tidak ada DQ check untuk identitas GROSS (qtyDeviasi ≟ COM−BOM) — bukan mismatch (master tidak mensyaratkan check; kolom sumber by-definition = COM−BOM), dicatat sebagai observasi. |
| 2 | "QTY Loss/Surplus (residual = Deviation − Waste − Susut − Trial)"; "\|QTY Deviation\| = \|Waste\| + \|Susut\| + \|Trial\| + \|Residual\|" | `engine/transform.ts:239-283` `computeResidual` (dipanggil `deriveRecord`:288, disimpan `process-rows-for-import.ts:337`); `metrics/deviation.ts:60-80` versi lib; KPI: `queries/dashboard.ts:384-388,279-280` (SUM(ABS(residualQty))) | **MATCH** | Residual DIHITUNG SEBAGAI SISA oleh konstruksi (bukan kolom input): `\|res\| = max(0, \|dev\| − (\|w\|+\|s\|+\|t\|))`, disimpan bertanda mengikuti tanda qtyDeviasi → identitas `\|dev\| = \|w\|+\|s\|+\|t\|+\|res\|` terjaga by construction; kasus over-explained (w+s+t > dev, identitas dilanggar DATA) → residual = 0 + `isOverExplained` → rule master #12 `OVER_EXPLAINED` (`rule-evaluation.ts:113`, `rules.yaml:191-199`, `validator.ts:171-196`) — persis semantik master "fraud red flag". Varian abs-each-then-sum (bukan `\|w+s+t\|` inner-sum) adalah perbaikan magnitudo campuran-tanda yang terdokumentasi (BUG-2-9 / FIX-DEEP-3C di komentar transform.ts) — ekuivalen dgn formula master saat komponen setanda. Dashboard: `kpisToBreakdown` (dashboard.ts:428-431) = waste+susut+trial+residual vs total=SUM(absQtyDeviasi); `DeviationBreakdownChart` menampilkan 4 kategori + tooltip formula identitas. |
| 3 | "Residual > 50% = most deviation UNEXPLAINED → indicator of fraud / input error." | Default: `settings/definitions.ts:96-110` (RESIDUAL_LOSS_WARN_PCT=0.50, RESIDUAL_LOSS_HIGH_PCT=0.70); `settings/runtime-thresholds.ts:84-85`; `config/thresholds.ts:12-13`; rules: `rules.yaml:202-227`, `rule-evaluation.ts:114-115` (`nls<0 AND residualRatio > thresholds.RESIDUAL_LOSS_HIGH_PCT/WARN_PCT`); display: `DeviationBreakdownChart.tsx:139-144` badge destructive saat `residual/total > 0.5` | **MATCH (spirit)** | Master TIDAK menetapkan angka default persis (tabel rule master memakai key `residualLossHighPct`/`residualLossWarnPct` tanpa angka). Nilai aktual: **WARN = 0.50, HIGH = 0.70** — boundary ">50%" master tercakup persis di level WARN (0.50) yang memicu RESIDUAL_LOSS_WARN + badge "sebagian besar deviation tidak terjelaskan" di chart pada rasio > 0.5; level ABNORMAL/fraud-flag di 0.70. Paritas threshold PENUH rule engine ↔ settings (`getRuntimeThresholds()` satu sumber; rule-evaluation memakai `thresholds.RESIDUAL_LOSS_*`), termasuk di compliance lens 3 (paritas sesuai master baris 406). `computePriority` P1: residualRatio > HIGH; P2: > WARN (`deviation.ts:281-291`). Konsisten, tidak diubah. |
| 4 | "LOSS = `nominalLossSurplus < 0` (actual > SOC → loss); SURPLUS > 0; NEUTRAL = 0" | Queries/rules: `queries/dashboard.ts:142-143,218-223,382-391`; `rule-evaluation.ts:114-116,360-366`; `health-ranking.ts:164-170`; `shared.ts:113-115` (DIRECTION_FROM_SUM_SQL); `peer-comparison.ts:207-209`; `by-other-metric-all-categories.ts:61-72`; `scripts/migrate-direction.ts:47-92` + `/api/migrate-direction`. Ingest: `metrics/deviation.ts:36-49` `computeDirection` via `engine/transform.ts:287` | **MATCH** | SEMUA gating LOSS/SURPLUS di queries + rules memakai `nominalLossSurplus < 0` (fallback ke qtyDeviasi HANYA bila NLS NULL — paritas SQL↔JS↔migrate-script). Ingest `computeDirection(qtyLossSurplus, qtyDeviasi)` memakai NET deviation di ruang QTY — sign-equivalent dgn `nominalLossSurplus` (nominal = qty × price, price ≥ 0), terdokumentasi di docstring + test `tests/lib/metrics.test.ts:43-65` ("returns LOSS when net (qtyLossSurplus) < 0"). Kandidat bug klasik (query memakai `nominalDeviasi` untuk arah) DICEK: grep `nominalDeviasi [<>]` di queries → hanya `ABS(SUM(nominalDeviasi)) > 0` (filter non-zero, bukan arah) → tidak ada. Display FE memakai field `direction` (dari NLS) — konsisten (2 observasi display di bawah). |
| 5 | Historical Z-Score "(SIGNED: positive=above mean=worse/red, negative=below=better/green). 2-metric selector: Dev/BOM ratio + QTY Deviasi (absolute). Only items with zScore > 0 shown as anomalous." | Komputasi (5 jalur): `metrics/historical.ts:96-101` (`computeZScore`), `:167-175` (`calcZScoreFromStats`), `queries/historical.ts:74-117` (stats ABS via 2-level CTE + `computeSampleStatsFromSums`), `queries/item-trend.ts:230-250` (signed, same-week, self-excluded), `rule-evaluation.ts:316-368` (`evaluateHistoricalRulesSql`: z=(ABS(pct)−mean)/stdDev, hanya z>0 yang fire), `app/api/analysis/services/post-process-historical.ts:120-146`, `queries/pareto/compute.ts:49`. FE: `lib/zScoreHelpers.ts:15-37`, `HistoricalZScoreCard.tsx:215-222` (filter), `ZScoreCell.tsx` | **MATCH** | z = (\|current\| − mean(\|hist\|)) / STDDEV_SAMP — SIGNED di semua jalur; baseline = minggu sama (W4 vs W4) bulan-bulan sebelumnya, current EXCLUDED (fetch-records.ts:251-256 `sortKey < current.sortKey`; item-trend byWeek+self-exclusion). FE `zScoreColor`: z>3/z>2/z>1 = red/amber/yellow; z<−2/z<−1 = emerald → positive=red, negative=green ✓. Filter FE: `activeZ > 0 && historicalAvg > 0 && |currentDevBom| ≤ 5` (guard BOM≈0) → hanya z positif tampil ✓. Selector 2-metric: `metricView: 'devBom' \| 'qtyDeviasi'` (waste/susut/trial dihapus per master baris 186) ✓. Rules hanya fire z positif: HISTORICAL_ABNORMAL (nls<0 ∧ z>zHigh), HISTORICAL_ABNORMAL_SURPLUS (nls>0 ∧ z>zHigh), HISTORICAL_WARNING (zWarn<z≤zHigh) ✓. `item-history/route.ts:262-275` juga exclude current + same-week. |
| 6 | Growth fields "computed in the rule-evaluation CTE using ABS(...) magnitude and div-by-zero guards" (wasteGrowth/susutGrowth/trialGrowth/bomGrowth/qtyDeviasiGrowth/salesGrowth/nominalDeviasiGrowth) | `rule-evaluation.ts:174-198` (CTE `g` CROSS JOIN LATERAL); lib: `metrics/growth.ts:31-35` (`calcGrowth`), `:43-49` (`calcGrowthAbs`), `:67-72` (`computeNominalDeviationGrowth`); konsumsi: `post-process-growth.ts:37-46` + `exec-summary.ts:38-45`; `growth-drivers/drivers.ts:272-279` | **MATCH** | CTE `g`: `salesGrowth = (c.sales − p.sales)/ABS(p.sales)`; `bomGrowth/qtyDeviasiGrowth/nominalDeviasiGrowth/wasteGrowth/susutGrowth/trialGrowth = (ABS(c)−ABS(p))/ABS(p)` — SEMUA 7 field dengan guard `IS NOT NULL AND != 0` → NULL (div-by-zero guard) ✓. Lib: `calcGrowth = (curr−prev)/\|prev\|`, prev=0 → null (0 bila keduanya 0); `calcGrowthAbs`/`computeNominalDeviationGrowth` = varian magnitudo (\|curr\|−\|prev\|)/\|prev\| — sesuai master "ABS(...) magnitude". `nominalDeviasiGrowth` dashboard pakai varian magnitudo (`computeNominalDeviationGrowth`, audit issue #11) konsisten dgn CTE. `growth-drivers` pakai `\|prev\|` denominator (BUG-2 fix). Semantik dikunci test: `src/lib/metrics/growth.test.ts` (21 test: guard null/0, ABS prev, magnitudo). |
| 7 (konteks) | "Outlet health ranking (3D)" + "Peer comparison ±10% sales" (agent lain) — komponen inti konsisten? | `health-ranking.ts:164-170` (direction dari nominalLossSurplus), zScore via `calcZScoreFromStats` (`:245` doc); residual/nominal KPI dari `dashboard.ts`/`outlet-agg-scan` | **MATCH (komponen inti)** | Komponen inti yang saya audit (direction, z-score, residual magnitudo, growth) konsisten di dalam health-ranking & peer pipeline — tidak ditemukan kontradiksi konvensi; verifikasi penuh 3D/±10% adalah domain agent lain. |

FIX YANG DILAKUKAN (diff minimal, NOL perubahan perilaku — hanya hygene + akurasi doc di domain sendiri):

1. `src/lib/metrics/deviation.ts` — hapus **unused import `toNum`** (lint `no-unused-vars` ×2 hilang; tidak pernah dipakai di file) + baris komentar stale "FIX: toNum imported from @/lib/format (deduplicated)". Docstring `computeResidual` + header Direction diperbarui ke bentuk aktual (abs-each-then-sum BUG-2-9) + referensi master tanpa numbering revisi lama.
   ```diff
   -import { toNum } from '@/lib/format';
   +import type { Direction } from './definitions';
    const safeDiv = ...   // (dipertahankan — helper aggregate tetap dipakai)
   -// Master context #9: from NET Deviation (qtyLossSurplus), fallback to GROSS
   +// Master context: Direction Convention — LOSS = nominalLossSurplus < 0
   - * Residual: Math.max(0, ABS(qtyDeviasi) - ABS(waste + susut + trial))
   - * Master context #11: Residual = sisa setelah W+S+T
   + * Residual: Math.max(0, ABS(qtyDeviasi) - (ABS(waste) + ABS(susut) + ABS(trial)))
   + * (abs-each-then-sum — BUG-2-9: benar untuk komponen campuran tanda)
   + * Master context: Residual = Deviation − Waste − Susut − Trial (sisa, bukan kolom input)
   ```
2. `src/lib/metrics/definitions.ts` — docstring `RESIDUAL` dikoreksi: "ABS(Net Deviation)" + "ABS(waste + susut + trial)" (bentuk pre-BUG-2-9, tidak akurat vs implementasi & master) → bentuk aktual + penjelasan bahwa residual dihitung sebagai SISA oleh konstruksi (bukan kolom input), sesuai klaim master baris 34+38.

Tidak ada mismatch fungsional kode-vs-master di domain metrik inti → tidak ada fix perilaku yang diperlukan.

DOC-STALE (kode menang, terdokumentasi — tidak diubah):
1. **MASTER_CONTEXT.md baris 284** menyebut 3 rule zScore via "JS post-process" — implementasi aktual `evaluateHistoricalRulesSql` (SQL push-down, PERF TAHAP-2/P2-7) dengan header komentar paritas semantik lengkap (baseline sama, z signed hanya-positive-fire, fallback EVAL-10). Bukti: `rule-evaluation.ts:256-293` (dokumen paritas), `rules.yaml:6-7`. Deviasi disengaja yang terdokumentasi (perf) → kode menang. (Domain VERIFY-RULES sudah menganotasi rules.yaml + komentar; saya tidak menyentuh.)
2. **definitions.ts/deviation.ts/historical.ts/growth.ts/sales.ts** memakai numbering "Master context #8/#9/#11/#19/#30-#33" dari revisi master LAMA — MASTER_CONTEXT.md saat ini tanpa numbering; semantik semua referensi tetap cocok dengan teks master baru. Komentar historis — dibiarkan (kecuali 2 yang saya perbaiki di atas karena keliru menggambarkan formula).
3. **definitions.ts RESIDUAL** (DIPERBAIKI sebagai doc, lihat fix #2) — bukti stale: implementasi abs-each (transform.ts:251-258, BUG-2-9) vs doc lama inner-sum.

OBSERVASI OUT-OF-DOMAIN (read-only, TIDAK difix — untuk pemilik domain masing-masing):
1. `src/app/api/item-history/route.ts:299-303` — fallback `isOverExplained` untuk computePriority memakai raw sum `qtyWaste + qtySusut + qtyTrial` (bukan abs-each-then-sum seperti transform.ts/validator.ts BUG-2-9). Hanya mempengaruhi baris dgn komponen campuran tanda; dampak = prioritas P1 (over-explained) bisa terlepas untuk baris tsb. Kandidat fix untuk owner route (bukan domain saya).
2. `PeerTableRow.tsx:45` mengirim `signedNominal: row.nominalDeviasi` (GROSS) ke `computeAnomalyFlags` → badge "LOSS tinggi" digating tanda GROSS, bukan NET (`nominalLossSurplus`). Setanda dgn NET pada data well-formed; domain VERIFY-DISPLAY.
3. Beberapa komponen display mewarnai baris via `nominalDeviasi < 0` (ItemPriorityPanel:94, PrioritySummaryCard:100, TopItems:37, ranking-nasional:143) — proxy arah GROSS; konsisten selama tanda gross=net (data produksi terverifikasi). Domain VERIFY-DISPLAY.

HASIL VERIFIKASI PASCA-FIX:
- `bunx tsc --noEmit --incremental false` → **0 error** (exit 0, seluruh repo).
- `bun run lint` → **0 error**; warnings 379 → **377** (2 warning unused-`toNum` di domain hilang; warning tersisa di tests/luar domain, diabaikan sesuai aturan).
- `bunx vitest run` (full suite) → **31 file / 512 test PASS** tanpa edit test; subset domain (`tests/lib/metrics/historical.test.ts`, `tests/queries/dashboard.test.ts`, `tests/lib/metrics.test.ts`, `src/lib/metrics/{growth,historical}.test.ts`, `tests/queries/{rule-evaluation,historical,price-effect,change-analysis,health-ranking}.test.ts`) → **10 file / 186 test PASS**.

Stage Summary:
- Audit VERIFY-CORE selesai: **7/7 klaim master di domain metrik inti = MATCH** (1 dengan catatan spirit: residual WARN=0.50 persis menangkap ">50%" master, HIGH=0.70 untuk fraud-flag; master tidak menetapkan angka default persis).
- Identitas dekomposisi \|dev\| = \|w\|+\|s\|+\|t\|+\|res\| terjaga **by construction** (residual selalu dihitung sebagai sisa di ingest — `transform.ts computeResidual` — dan KPI memakai nilai tersimpan; over-explained di-flag sesuai rule master #12).
- Direction LOSS = `nominalLossSurplus < 0` konsisten di SELURUH queries/rules/migrate (fallback qtyDeviasi hanya bila NLS NULL); ingest memakai qtyLossSurplus (sign-equivalent, terdokumentasi + dites). Tidak ditemukan query yang memakai nominalDeviasi untuk arah.
- Z-Score SIGNED konsisten di 5 jalur komputasi + FE (positive=red, filter hanya z>0, selector 2-metric Dev/BOM + QTY Deviasi); growth = (cur−prev)/\|prev\| dgn guard div-by-zero di lib + 7 field CTE rule-evaluation memakai ABS magnitudo + guard.
- 2 fix kosmetik domain (unused import + doc formula residual yang stale pre-BUG-2-9) — nol perubahan perilaku; 3 DOC-STALE dicatat (1 di MASTER_CONTEXT.md sendiri: mekanisme rule zScore JS→SQL), 3 observasi out-of-domain dilaporkan untuk agent pemiliknya.
- tsc 0 error · lint 0 error (domain bersih) · 512/512 test pass · tidak ada commit/push · worklog.md tidak disentuh.
