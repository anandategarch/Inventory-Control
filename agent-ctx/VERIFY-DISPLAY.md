---
Task ID: VERIFY-DISPLAY
Agent: full-stack-developer
Task: Audit kecocokan implementasi vs MASTER_CONTEXT.md (domain: Analytics tampilan/perhitungan kartu — efficiency score, peer ±10%, pattern badge, pareto 80/20, heatmap dual display, rank badges, heatmap cell-detail, pareto 5 dimensi, ItemPeerComparison 4 kartu). Working repo `/home/z/audit-inventory` saja; DB/dev/build tidak dijalankan; file agent lain tidak disentuh.

Work Log:

1. Baca MASTER_CONTEXT.md klaim domain (baris 318-341 §Analytics + §Trend Item Tab Expansion) + PRD_RETROAKTIF.md §6.4 (spesifikasi detail ItemPeerComparison, baris 598-645). Petakan file aktual domain (`rg --files`): peer-computation.ts, item-peer-compute.ts, ItemPeerComparison/{card-compute,PeerTableRow,index}.tsx, shared/peer-comparison-cards/*, ItemTrendTable/{patternBadge,ItemTrendTableHeader,index}.tsx, RankBadgeRow.tsx, ItemTrendRankChart.tsx, src/lib/queries/heatmap.ts, src/lib/queries/pareto/*, src/app/api/{area-item-heatmap,pareto,peer-comparison,item-peer-comparison}/**.
2. Verifikasi 10 klaim satu per satu (baca kode + git history `git show 7f455f4 / 93f76c0 / a70d1c8` untuk menelusuri asal-usul deviasi + worklog.md untuk bukti deviasi terdokumentasi). Detail per klaim di tabel Stage Summary.
3. Temuan: 9/10 klaim MATCH; 1 MISMATCH perhitungan pada heatmap cell-detail ("Ø PER RESTO (avg per outlet)" membagi dengan jumlah baris outlet×akunPenyesuaian, bukan jumlah outlet DISTINCT) → diperbaiki (diff minimal, 1 file).
4. Post-fix: `bunx tsc --noEmit --incremental false` → 0 error; `bun run lint` → 0 error (warning baseline, tidak ada warning baru di file yang diubah); `bunx vitest run` full suite 512/512 pass (termasuk domain: pareto 10, item-outlet-breakdown 10, top-items 12, metrics 61). Tidak ada test yang diedit.
5. Tidak commit/push; tidak menulis worklog.md. File lain yang ter-modify di working tree (`src/config/rules.yaml`, `src/lib/queries/rule-evaluation.ts`) adalah pekerjaan agent paralel VERIFY-RULES — tidak disentuh.

TABEL VERIFIKASI PER KLAIM:

| # | Klaim (verbatim master) | Lokasi aktual | Hasil | Bukti |
|---|--------------------------|---------------|-------|-------|
| 1 | EfficiencyScoreCard `100 - (devBomPenalty + nominalPenalty)`, tiap penalty bounded [0,50], ABS magnitude (BUG-2-03) | `src/components/dashboard/tabs/ItemTrendTab/item-peer-compute.ts:24-38` | **MATCH** | `devBomPenalty = Math.min(50, Math.max(0, safeDiv(targetAbsDevBom - peerAbsDevBom, peerAbsDevBom) * 25))` (baris 29-31, ABS via `Math.abs` baris 27-28, komentar FIX BUG-2-03 baris 19-21); `nominalPenalty` idem clamp [0,50] (32-35); `raw = 100 - (devBomPenalty + nominalPenalty)` (36) + clamp final [0,100] (37). Dipakai di `ItemPeerComparison/index.tsx:163` → `<EfficiencyScoreCard score={score}>` (204-207). Peer Tab memakai formula 4-penalty berbeda (`peer-computation.ts:16-25`) — sesuai master baris 222 "Item Tab vs Peer Tab formulas differ, caller handles". |
| 2 | Peer comparison: outlet vs ±10% sales peers | BE `src/lib/queries/outlets/peer-comparison.ts:220-221`; `peer-comparison-items.ts:166`; `src/app/api/peer-comparison/trend/route.ts:100-110`; FE `PeerComparison/index.tsx:102,116` | **MATCH** (range memang ±10%, bukan ±15) | SQL: `AND ABS(COALESCE(sm.sales,0) - t.sales) <= CASE WHEN t.sales > 0 THEN t.sales * 0.1 ELSE 999999999 END` (= band ±10% dari sales target). Route trend auto-compute memanggil `queryPeerComparison` yang sama ( komentar "the ±10% sales band matches the main peer table"); route items memakai predikat identik `t.sales * 0.1`. FE label: "resto dengan sales ±10%". Item Tab peer memakai bucket BOM ±50% (`peer-bucket.ts` [0.5×,1.5×]) — mekanisme berbeda yang memang didokumentasikan master baris 146. |
| 3 | patternBadge: Massal ≥10 (🔴) / Regional 5-9 (🟡) / Lokal 2-4 (⚪) / Tunggal =1 (⚪) | `ItemTrendTable/patternBadge.ts:11-48`; dipakai di `ItemTrendTable/index.tsx:109`; kolom "Pola" di `ItemTrendTableHeader.tsx:82-86` | **MATCH** (threshold + operator) + **DOC-STALE** (emoji Tunggal) | `>=10` Massal 🔴 (12-17); `>=5` Regional 🟡 (19-24); `>=2` Lokal ⚪ (26-31); `===1` Tunggal (35-41). Operator presisi: 5-9→`>=5`, 2-4→`>=2`. Emoji Tunggal aktual **📍** bukan ⚪ — komentar baris 34 "FIX (BUG-1-03): use distinct emoji for Tunggal (was same ⚪ as Lokal)" (deviasi disengaja terdokumentasi → DOC-STALE, lihat bawah). `outletCount===0` → N/A (BUG-1-02). |
| 4 | RankBadgeRow: lookup `topDeviasiRank` by itemName, `Math.min` across rows; 3 badge; red ≤5 / amber 6-20 / muted >20; BOM rank nullable utk qtyBom=0 (BUG-1-01); fallback "> 50 Nasional" | `src/components/dashboard/tabs/ItemTrendTab/RankBadgeRow.tsx` (di-render dari `components/TrendHeader.tsx:159-161`) | **MATCH** | Filter `it.itemName === itemName` (39); `Math.min(...matches.map(m => m.rankNominal))` (45-47); `rankBom` nullable — nulls difilter type-predicate lalu `Math.min` (48-53, komentar FIX BUG-1-01); `rankBadgeClass`: `rank<=5` red / `rank<=20` amber / `>20` atau null atau `>50` muted (24-28); 3 badge: "Rank #N Nasional (Deviasi)" (63) + "Rank #M (BOM)" (70) + "N outlet terdampak (top 50)" (73); fallback `'Rank > 50 Nasional'` (63) + `'Rank BOM > 50'` (70). BE nullable: `by-deviasi-rank.ts:151-153` "preserve null — SQL returns NULL for qtyBom=0 items" + hook type `useAnalysis/types.ts:73-77`. |
| 5 | Rank Trend chart: Y INVERTED via `reversed`; dot red ≤5 / amber 6-20 / muted >20; ReferenceLine y=5 (red) + y=20 (amber) hanya saat maxRank ≥ threshold | `src/components/dashboard/tabs/ItemTrendTab/ItemTrendRankChart.tsx` | **MATCH** | `<YAxis reversed domain={[1, maxRank]} allowDataOverflow ...>` (236-246); `rankColor`: `<=5` → `#dc2626`, `<=20` → `#f59e0b`, else `var(--muted-foreground)` (88-92, komentar FIX BUG-3-01 selaras dengan badge); `{maxRank >= 5 && <ReferenceLine y={5} stroke="var(--chart-loss)" strokeDasharray="2 4" .../>}` (253-255) + `{maxRank >= 20 && <ReferenceLine y={20} stroke="var(--chart-waste)" .../>}` (256-258); `--chart-loss:#dc2626` (red) + `--chart-waste:#f59e0b` (amber) di `globals.css:80-82`. Recharts LineChart, tinggi `h-[100px]` (214), click → drillPeriod (159-164). |
| 6 | Heatmap: Pareto 80% default + selector 'Pareto 80%'/'Top N'; paretoInfo {totalItems, selectedItems, cumulativePct, totalMagnitude}; fallback Top-N utk pctQtyDeviasiToBom + recordCount; dual display Total (bold) + Ø avg (muted); outletCount COUNT(DISTINCT); avg hanya 3 nominal metrics; raw qty fields; h-11; tooltip Total+Avg+Outlet+Record | BE `src/lib/queries/heatmap.ts`; API `src/app/api/area-item-heatmap/route.ts`; FE `AreaItemHeatmap/*` | **MATCH** | BE: default `mode: ItemSelectMode = 'pareto80'` (87); `ItemSelectMode` type (33); `paretoInfo` 4 field persis (60-65); smart fallback `mode==='pareto80' && (metric==='pctQtyDeviasiToBom' || metric==='recordCount') → 'top'` (99-102); loop kumulatif ≥80% (137-153); `COUNT(DISTINCT ir."outletId") as "outletCount"` (181); raw `qtyBom/qtyDeviasi/qtyWaste/qtySusut/qtyTrial` + `nominalDeviasi/nominalLossSurplus` (182-188). API: `mode` default 'pareto80' (62), divalidasi (82), masuk cache key (103). FE: selector "Pareto 80%"/"Top N" (`HeatmapControls.tsx:42-43`); state default 'pareto80' (`index.tsx:73`); banner paretoInfo 4 field (228-247); dual display Total `font-semibold` atas + `Ø` muted bawah (`HeatmapCellView.tsx:57-67`); `AVG_ELIGIBLE_METRICS = {absNominalDeviasi, nominalWaste, nominalSusut}` — pct & recordCount EXCLUDED (`heatmapHelpers.ts:62-64`); `computeAvgPerOutlet = value/outletCount` (66-69); `h-11` (48); tooltip: metrik+Total, "Rata-rata per resto", "Jumlah resto", "Jumlah record" (`HeatmapTooltip.tsx:53-66`). |
| 7 | Cell-detail drill-down: footer TOTAL row + Ø PER RESTO row (avg per outlet); header aggregate summary incl. Ø per Resto column | BE `heatmap.ts:239-284` + `api/area-item-heatmap/cell-detail/route.ts`; FE `AreaItemHeatmapSheet.tsx` | **MISMATCH → FIXED** (denominator Ø) | Struktur MATCH: header 4 tile agregat dengan tile "Ø per Resto" (166-170); tfoot TOTAL row (233-243) + Ø PER RESTO row (245-254). Perhitungan MISMATCH: `outletCount = rows.length` padahal rows = per (outlet × akunPenyesuaian) → outlet multi-akun (fenomena nyata, worklog "multi-akunPenyesuaian items" mis. ADJUSTMENT+INVENTORY) menggelembungkan pembagi → rata-rata per resto UNDERSTATED & "Total Resto"/"TOTAL (N resto)" OVERSTATED vs klaim master "avg per outlet". Fix: `new Set(rows.map(r => r.outletCode)).size` (lihat diff). |
| 8 | Pareto 80/20 analysis 5 dimensi (Item, Outlet, Area, Kelompok, PIC) | BE `src/lib/queries/pareto/by-dimension.ts` + `types.ts:29` + `api/pareto/route.ts`; UI `ParetoDashboard.tsx` + `constants.ts` | **MATCH** | 5 fungsi query: `queryParetoByItem` (16) / `ByOutlet` (50) / `ByArea` (83) / `ByKelompok` (118, prefix 3-char dari segmen terakhir kode outlet) / `ByPIC` (156, COALESCE 'Unassigned'); `ParetoDimension = 'item'\|'outlet'\|'area'\|'kelompok'\|'pic'` (types.ts:29); route menjalankan kelima nya paralel + `nestedGeneralized` parentDim/childDim (route.ts:111-119, 154-161). UI: `DIM_LABELS` 5 entri (constants.ts:9-15) + 2 Select parent/child over 5 dimensi (ParetoDashboard.tsx:147-182). |
| 9 | ItemPeerComparison 4 kartu: Gap (target vs peerAvg vs peerBest + Dev/BOM row); Scatter (BOM,Deviasi) target amber ring + prepended (BUG-2-01/02); RankingSummary rank + p percentile; tabel flags Normal/Near Peer Avg/Anomali | `ItemPeerComparison/card-compute.ts` + `shared/peer-comparison-cards/{gap-analysis,scatter-plot,ranking-summary,anomaly-flags}.tsx` + `PeerTableRow.tsx` + BE `src/lib/queries/items/item-peer-comparison.ts` | **MATCH** (per PRD §6.4 — spesifikasi detail yang diikuti kode; "Near Peer Avg" = parafrasa kriteria ±20%) | Gap: 3 row (Nominal Deviasi / QTY Deviasi / **Dev/BOM**) masing-masing `targetVal`/`bestVal`/`avgVal`, best = non-target `Math.min(absNominalDeviasi)` (BUG-2-04, card-compute.ts:37-69). Scatter: x=`qtyBom`, y=`absNominalDeviasi` (75-78); target fill `#f59e0b` + ring stroke `#fbbf24` strokeWidth 2 + `r={7}` vs `r={4}` (scatter-plot-card.tsx:147-161); target masuk `peers[]` server-side + ORDER BY target-first + komentar "FIX (BUG-2-01 + BUG-2-02): INCLUDE target in peers[]" (item-peer-comparison.ts:313-322, 349-356); tabel FE render baris target PERTAMA (index.tsx:261-267). RankingSummary: `#rank/total` by absNominalDeviasi asc + `p${percentile}` (card-compute.ts:96-118). Flags threshold MASUK AKAL & KONSISTEN: 🔴 Anomali `|devBom| > 1.5× ABS(peerAvg.devBom)` / LOSS + `absNominal > 1.5× peerAvg` — 🟢 Normal "near peer avg" = tanpa flag DAN dalam ±20% peer avg di 2 metrik (anomaly-flags.tsx:88-115, ABS-corrected per BUG-2-05/06; PRD §6.4 baris 634-637 identik). Zona antara 1.2×–1.5× → "—" (by design PRD). Row click → `onOutletClick` → Resto tab ✓. |
| 10 | Test terkait pass tanpa edit test | `tests/queries/{pareto,item-outlet-breakdown,top-items}.test.ts`, `tests/lib/metrics.test.ts` | **PASS** | Pre-fix: 32/32 (pareto 10 + breakdown 10 + top-items 12). Post-fix: 93/93 (+metrics 61). Full suite: **512/512**. Nol file test diubah. |

FIX YANG DITERAPKAN (1 file, diff minimal):

`src/components/dashboard/AreaItemHeatmapSheet.tsx` (FIX VERIFY-DISPLAY — denominator "per resto"):
```diff
-  const outletCount = rows.length;
+  // FIX (VERIFY-DISPLAY): rows are per (outlet × akunPenyesuaian) — a multi-akun
+  // outlet (e.g. "ADJUSTMENT" + "INVENTORY") contributes MULTIPLE rows. "Total
+  // Resto" / "Ø PER RESTO" must use the DISTINCT outlet count (master context:
+  // "Ø PER RESTO row (avg per outlet)"), not the row count — dividing by
+  // rows.length understated the per-resto average for multi-akun outlets.
+  // Plain const (no useMemo) to match the original non-hook form; rows ≤ 1000.
+  const outletCount = new Set(rows.map((r) => r.outletCode)).size;
```
```diff
-                <div className="text-sm font-semibold">{rows.length}</div>   <!-- tile "Total Resto" -->
+                <div className="text-sm font-semibold">{outletCount}</div>
-                      <td className="py-2 px-1.5">TOTAL ({rows.length} resto)</td>
+                      <td className="py-2 px-1.5">TOTAL ({outletCount} resto)</td>
```
Efek: `avgNominal`/`avgQtyDeviasi`/`avgQtyBom` (Ø PER RESTO row + tile "Ø per Resto") kini membagi total dengan jumlah outlet DISTINCT sesuai klaim master "avg per outlet"; tile "Total Resto" & label "TOTAL (N resto)" tidak lagi menghitung baris akun ganda sebagai resto ekstra. TOTAL row tetap penjumlahan semua baris (semantics tidak berubah). Kolom Ø Dev/BOM di footer (avgQtyDeviasi/avgQtyBom) ikut terkoreksi otomatis.

DOC-STALE (deviasi kode vs master yang disengaja + terdokumentasi → kode TIDAK diubah):

1. **Tunggal emoji 📍 vs master "⚪"** (MASTER_CONTEXT:333). Bukti: `patternBadge.ts:33-34` komentar "FIX (BUG-1-02): outletCount=0 ... / FIX (BUG-1-03): use distinct emoji for Tunggal (was same ⚪ as Lokal)". Master sendiri menulis "with BUG-1-02/03 cosmetic fix documented" namun simbol lama ⚪ belum diganti di teks master.
2. **"13 fields per row" vs aktual 14** (MASTER_CONTEXT:320). Bukti: `HeatmapCellDetailRow` (AreaItemHeatmapSheet.tsx:35-50 / heatmap.ts:222-237) punya 14 field — `nominalLossSurplusSigned` ditambahkan commit `93f76c0` ("FE-02+03: ... added nominalLossSurplusSigned field ...") untuk fix isLoss-selalu-false; terdokumentasi di commit message + worklog.
3. **Lokasi file master usang** (MASTER_CONTEXT:332-333): "`RankBadgeRow` in `index.tsx`" → aktual `RankBadgeRow.tsx` (REFACTOR-1-b); "`patternBadge()` in `ItemTrendTable.tsx`" → aktual `ItemTrendTable/patternBadge.ts` (SPLIT-B + BUG-B-fix, agent-ctx/SPLIT-B-split.md & agent-ctx/BUG-B-fix.md — pure code motion, paritas penuh). Tree master (baris 568) masih menggambarkan folder flat pra-split.
4. **TTL cache `heatmap-cell-detail` "60 dtk" vs aktual 5 menit** (MASTER_CONTEXT:418). Bukti: `cell-detail/route.ts:29` `HEATMAP_CELL_CACHE_TTL = 5 * 60 * 1000; // 5 min`; commit `a70d1c8` pesannya "QW6b (heatmap/cell-detail): withCacheAndDedup **5-min TTL**" (angka 60 dtk adalah QW6a item-search, tertukar di teks master).
5. (Catatan ringan, bukan stale penuh) **"Near Peer Avg"** (MASTER_CONTEXT:340) hanya muncul di master — tidak pernah ada di kode/worklog/git-history (`git log -S "Near Peer"` → hanya commit dok 7f455f4). PRD §6.4 (commit yang SAMA) mendefinisikan flag aktual: 🔴 Dev/BOM tinggi / 🔴 LOSS tinggi / 🟢 Normal (±20% peer avg) — kode mengikuti PRD verbatim. "Near Peer Avg" = parafrasa kriteria ±20% "near peer average" dari badge Normal; threshold vs Anomali (1.5×) masuk akal & konsisten (penyebut peerAvg sama, ABS-corrected BUG-2-05/06). Zona 1.2×–1.5× tampil "—" sesuai desain PRD.

VERIFIKASI PASCA-FIX:
- `bunx tsc --noEmit --incremental false` → **0 error** (exit 0, tanpa output; seluruh repo, domain 0).
- `bun run lint 2>&1 | tail -30` → **0 errors** (377-380 warning = baseline pre-existing; `AreaItemHeatmapSheet.tsx` tetap 7 warning yang sama seperti pre-fix — 6× react-hooks/exhaustive-deps `rows` + 1 unused `v`; tidak ada warning baru dari fix).
- Test: `bunx vitest run tests/queries/pareto.test.ts tests/queries/item-outlet-breakdown.test.ts tests/queries/top-items.test.ts tests/lib/metrics.test.ts` → **93/93 pass**; full suite `bunx vitest run` → **512/512 pass** (31 file). Tidak ada test diedit.

Stage Summary:
- 10 klaim domain diverifikasi terhadap MASTER_CONTEXT (+PRD §6.4 sebagai spesifikasi detail): 9 MATCH penuh, 1 MISMATCH perhitungan (heatmap cell-detail Ø PER RESTO membagi per baris outlet×akun, bukan per outlet DISTINCT) → fixed dengan diff minimal 1 file (`AreaItemHeatmapSheet.tsx`: `outletCount` kini `Set(outletCode).size`, dipakai tile "Total Resto", "TOTAL (N resto)", dan seluruh nilai Ø).
- 5 DOC-STALE tercatat dengan bukti (emoji Tunggal 📍/BUG-1-03; 13→14 field cell-detail/commit 93f76c0; lokasi file RankBadgeRow/patternBadge pasca SPLIT-B; TTL cell-detail 5 mnt bukan 60 dtk/commit a70d1c8; "Near Peer Avg" = parafrasa PRD ±20%).
- Gerbang kualitas: tsc 0 err · eslint 0 err (warning baseline) · vitest 512/512. Tanpa commit/push, tanpa worklog.md, tanpa menyentuh file domain agent lain (rules.yaml & rule-evaluation.ts sedang diubah VERIFY-RULES — dibiarkan).
