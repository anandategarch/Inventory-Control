Task ID: PEERTOP-2-b
Agent: general-purpose (PDF export)
Task: Export report section 8 — add 8.3 (top item resto setara, bersama vs khusus) + 8.4 (top item per resto setara)

Work Log:
- Context read: worklog tail (master-context rules: ABSOLUTE averages labeled "Rata-Rata …", NO "pp", NO sales nominals in the PDF, REFINE-4 "resto setara" terminology), src/lib/queries/outlets/peer-top-items.ts (result contract), sections/peer.ts + data-fetcher/peer.ts + services/types.ts, format-helpers (fmtIDR/fmtNum/fmtPct signatures — fmtIDR null/undefined → '—').
- WRAP CHECK (decides 8.4 layout) — read pdf-primitives.ts `Rpt.table` (line 470): cells DO wrap. `isWrap = cols.map((c) => c.wrap ?? (c.align ?? 'left') === 'left')` (line 490) — left-aligned columns wrap by default, any column can force `wrap: true`; body cells wrap via `wrapLines` (lines 559-562), header cells ALWAYS wrap (lines 555-556); nothing is ever ellipsized (FIX-TERPOTONG auto-fit: min width = longest unbreakable word, font steps down only when even that overflows). → 8.4 uses the COMPACT layout (one row per outlet, items joined in one wrapping cell).
- types.ts: added `PeerTopItemRow` (8.3 union row: itemName, satuan, peerTopCount, peerAvgAbsNominal, peerMaxAbsNominal, target{rank,absNominal,devBom,direction}|null) + `PeerTopItemOutletRow` (8.4 entry: outletCode/outletName/isTarget/items[{itemName,absNominal,devBom,direction}]) and extended `PeerComparisonData` with OPTIONAL `topItems?` / `peerTopItems?` (additive — existing consumers unaffected; absent = fetch failed/skipped, [] = fetched but no deviation records → section skips). Mapped payload carries NO sales values (SALES SECRECY).
- data-fetcher/peer.ts: third dependent call inside `if (targetRow)`, same cachedSharedQuery pattern: `('q-peer-topitems', { month, week, filters: filterOpts, extra: { target, topN: 5, limit: 10, sv: 1 } }, () => queryPeerTopItems(targetCode as string, month, week, 'week', 5, 10, kelompokParam))` — limit 10 matches the existing queryPeerComparison call so the band = exactly the restos in 8.1; stays inside the same try → failure leaves peerComparison null (non-fatal semantics unchanged). Mapping: topItems = topRes.items.slice(0, 10) (union rows, server sort preserved); peerTopItems = perPeer REORDERED by walking peerRes.peers (sales proximity) with a Map lookup by outletCode — peers without an entry (zero deviation records) simply skipped. Import `queryPeerTopItems` from '@/lib/queries' barrel (already re-exported by PEERTOP-1).
- sections/peer.ts (8.1/8.2 untouched): 8.3 guard `pc.topItems && length > 0` (same style as 8.2); subhead `8.3 Top Item Resto Setara — bersama vs khusus (top item = jumlah |nominal deviasi| per item; resto setara = penjualan ±10%)` — the definition rides in the subhead because NO footnote helper exists in section modules (noteBox is for empty-data notes; rpt.para is used by no section) per the spec's fallback rule. Cols: # (center) | Item | Top di (right, "m dari n resto setara", n = pc.peers non-target count) | Rank di Target (right: #r ≤ 5 / "> 5" / —) | Nominal Target (right, fmtIDR, — when null) | Rata-rata Absolute Resto Setara (right, fmtIDR, — when peerTopCount 0 — header contains "Rata-rata Absolute" per master context) | Arah (center). Arah cellColor: LOSS → C.danger, SURPLUS → C.success (per-cell convention as 6.2/anomaly; Nominal Target stays neutral — it is an ABSOLUTE magnitude so 8.1's rowText('-') rule can never apply). No rowBold (target is a column, not a row).
- 8.4 guard `pc.peerTopItems && length > 0`: target entry first (isTarget) + up to 5 non-target entries in the fetcher's sales-proximity order → ≤6 outlets × ≤5 items. COMPACT table (wrap confirmed): cols Resto | Top Item (nominal, arah); items joined `1. NAME (Rp X, LOSS); 2. …` via fmtIDR; rowBold for the target row (mirrors 8.1); empty item list fallback '—'. Top file comment block updated with 8.3/8.4 (PEERTOP) entries.
- Width verification (pdfkit font metrics via node script, artifact deleted): 8.3 subhead = 505.5pt and 8.4 = 246.9pt vs content width 511.28pt → both fit (the pre-existing 8.2 subhead already overflows with long outlet names — 534.8/609.5pt — untouched per "8.1/8.2 unchanged" rule, no new overflow introduced).
- Gates: `bunx tsc --noEmit --incremental false` → exit 0; `bun run lint` → 0 errors / 377 warnings (baseline 377-379; ZERO warnings in the 3 touched files — all 377 are pre-existing in other files).

Stage Summary:
- Files changed (exactly the 3 allowed): src/app/api/export-report/services/types.ts (+44), src/app/api/export-report/services/data-fetcher/peer.ts (+45), src/app/api/export-report/services/pdf/sections/peer.ts (+88). No other files touched (other working-tree modifications belong to PEERTOP-1 / PEERTOP-2-a).
- Layout decision: rpt.table cells WRAP (left-aligned default, `wrap` opt-in, never truncated — pdf-primitives.ts:490/559) → 8.4 = compact one-row-per-outlet with joined item list in a wrapping cell (NOT the stacked per-item format).
- 8.3 renders the cross-peer union (bersama vs khusus + target blind spots via "—" rank), 8.4 renders per-resto top items with the target row bold; both use "resto setara" terminology, "Rata-rata Absolute" labeling, Indonesian headings, and print no sales values.
- tsc clean (exit 0); lint 0 errors, 377 warnings = no new issues. DB unreachable → static verification only (per task rules: no vitest/build/dev-server/commit).
