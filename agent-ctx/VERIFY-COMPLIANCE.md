---
Task ID: VERIFY-COMPLIANCE
Agent: full-stack-developer
Task: User: "Cek apakah perhitungan semua sudah sesuai master context" — domain Kontrol & Kepatuhan (tab ke-6, 11 lensa dari 2 scan). Audit kecocokan implementasi vs MASTER_CONTEXT.md baris 383-406 (kutipan verbatim PAKET E): `queryComplianceDashboard` (CTE base → 6 agregat → materialized SEKALI → UNION ALL `(lens, to_jsonb(row))`), `queryChronicOutlets` (CTE wk → 3 CTE + lensa 'week'), 11 lensa, paritas threshold penuh `getRuntimeThresholds()`.

Work Log:
- Baca MASTER_CONTEXT.md 383-406 + sekitarnya (135, 141-142, 189, 203, 521, 549-570, 595) — spesifikasi PAKET E lengkap.
- Cari implementasi: `src/lib/queries/compliance.ts` TIDAK ADA; `src/lib/queries/chronic-outlets.ts` TIDAK ADA; `src/app/api/compliance/**` & `src/app/api/chronic-outlets/**` TIDAK ADA; `src/components/**/Compliance*` TIDAK ADA. `rg -i compliance src/` → 0 file.
- Telusuri git: PAKET E pernah ADA — dibangun berurutan `4cd3e76` (tab + 6 lensa) → `5964ba8` (lensa 7 antar-area) → `66f8c34` (lensa 8 kronis) → `02ec42c` (lensa 9 angka bulat) → `ea18e0e` (lensa 10 momentum) → `c01c268` (lensa 11 drilldown) → docs `9cf232f` → shortcut `7572ee1` — lalu **DIHAPUS TOTAL di commit `b37cc40` "refactor(ui): hapus tab Kepatuhan + endpoint /api/compliance & /api/chronic-outlets"** (−2.268 LOC: Compliance.tsx 804, ComplianceTab.tsx 23, api/compliance 233, queries/compliance 741, chronic-outlets pair 467; sinkronisasi invalidasi query key, shortcut keyboard '6', routes list aggregation-cache; "nol konsumen/test lain — terverifikasi grep + build output").
- Kronologi: `b37cc40` (Sep 11 00:30) berada 9 commit SETELAH penulisan spec (`9cf232f`) dan 63 commit SEBELUM HEAD (`e1cfd0f`) — 63 commit sesi berikutnya (H-8..H-13, VH-0, SPLIT-A..G, BUG-HUNT-1/2) seluruhnya dibangun di atas state pasca-penghapusan.
- Klasifikasi deviasi: **sengaja** (judul commit eksplisit + bagian rangkaian penghapusan "per user request" — bukti `ExportDialog.tsx:17-18` "H-5: '5. Analisis Korelasi BOM' removed per user request (same removal series as Loss-to-Sales / Kepatuhan)"; saudara sesi `54c20ab` hapus kartu KPI Total LOSS/SURPLUS), **terdokumentasi** (komentar kode `src/lib/aggregation-cache/invalidate.ts:100` "H-2b: removed the two Kepatuhan-tab routes (tab deleted with its APIs)" + MASTER_CONTEXT.md:135 "(Earlier removals: ... `/api/compliance` + `/api/chronic-outlets`, H-2b Kepatuhan-tab deletion.)"), **lebih baru** dari spec (bukti kronologi di atas). → Sesuai ATURAN FIX: **DOC-STALE — jangan ubah kode** (mengembalikan 2.268 LOC juga bukan "diff minimal").
- Audit perhitungan pra-penghapusan (untuk membuktikan spec pernah akurat): ekstrak via `git show b37cc40^:<path>` ke /tmp (nol penulisan ke repo), baca rule-evaluation.ts (READ-ONLY, domain VERIFY-RULES) untuk paritas, baca config/thresholds.ts + engine/transform.ts untuk definisi residual.
- Cek pengimplementasian ulang dengan nama lain: `rg "tol_item|transfer_agg|crossArea|roundOutlets|chronicCount|momentumPct|KRONIS|nRound5|breachRatePct" src/` → 0 match. Mini-services/scripts → 0 match.
- Cek test: `rg -l -i "compliance|chronic" tests/` → TIDAK ADA file test compliance/chronic (exit 1). Tidak ada yang dijalankan (sesuai instruksi #12).
- Verifikasi pasca-audit (tanpa perubahan kode): `bunx tsc --noEmit --incremental false` → exit 0, 0 error. `bun run lint` → 0 error, 379 warning (semua pre-existing di tests/, baseline sama dengan worklog BUG-HUNT-2). Domain error = 0.

---

## TABEL VERIFIKASI 11 LENSA + STRUKTUR SQL

**Status di HEAD (`e1cfd0f`): SELURUH 11 lensa + 2 query + 2 route + komponen TAB TIDAK ADA (dihapus H-2b).** Kolom "pra-penghapusan" = audit kode dari git `b37cc40^` (induk commit penghapusan) — bukti spec pernah diimplementasikan sesuai master SEBELUM dihapus atas permintaan user.

| # | Claim Master (383-406) | Lokasi kode di HEAD | Status di HEAD | Pra-penghapusan (b37cc40^) | Bukti |
|---|------------------------|---------------------|----------------|---------------------------|-------|
| 1 | Flag `f_tol_*` paritas rule engine `getRuntimeThresholds()` yang SAMA (stdDevBomPct / 2× / \|Dev/BOM\| vs \|tolerancePct\|), bukan hardcode | — (file dihapus) | **MISMATCH (absent)** | **MATCH** | queries/compliance.ts L279-288: `nBreach` = `tolPct NOT NULL ∧ pct NOT NULL ∧ ABS(pct)>ABS(tol)`; `nBreachHigh` = `> 2*ABS(tol)`; `nNotSetHigh` = `tolPct NULL ∧ ABS(pct) > ${thresholds.stdDevBomPct}` — identik rule-evaluation.ts L110-112 (`f_tol_breach_high`/`f_tol_breach`/`f_tol_not_set`, `ABS(NULL)>x` NULL-falsy ≡ eksplisit IS NOT NULL). Route api/compliance/route.ts L196+L208-212: `getRuntimeThresholds()` → `{stdDevBomPct: thresholds.STD_DEVIASI_BOM_PCT, ...}` — nilai di-inject, 0 hardcode. Nuansa terdokumentasi: rule engine mem-filter populasi `absNominalDeviasi > 0` (rule-evaluation.ts L78), `base` compliance tidak — edge-case record nominal-dev=0 tapi pctDev/BOM>tol terhitung di panel saja; tidak menyentuh nilai threshold. |
| 2 | `tolerancePct` NULL + deviasi besar | — (dihapus) | **MISMATCH (absent)** | **MATCH** | compliance.ts L292-295 (`nNoTol` = `COUNT FILTER tolerancePct IS NULL`, `nominalNoTol`, `nNotSetHigh` pakai stdDevBomPct) + shaping L492-505: filter `nNoTol>0 ∧ (nominalNoTol>0 ∨ nNotSetHigh>0)`, sort `nominalNoTol DESC, nNotSetHigh DESC`, slice 10 — "daftar aksi tetapkan toleransi dulu". |
| 3 | residual = dev − (waste+susut+trial); WARN/HIGH paritas rule (`residualWarnPct`/`residualHighPct`) | — (dihapus) | **MISMATCH (absent)** | **MATCH nilai threshold; 1 nuansa bentuk kondisi** | Residual dibaca dari kolom pre-computed `residualNominal` (= dev − (\|w\|+\|s\|+\|t\|), clamp ≥0, engine/transform.ts L239-283) — sesuai rumus master. Shaping L525-529: `share > residualHighPct*100 → HIGH; > residualWarnPct*100 → WARN` dengan share = residualNominalAbs/absNominalDev×100; fraction (thresholds.ts: WARN 0.50 / HIGH 0.70) ×100 konsisten dengan rule `residualRatio > RESIDUAL_LOSS_*_PCT` (band warn `>warn ∧ ≤high` ekuivalen). Route L210-211 pass `RESIDUAL_LOSS_WARN_PCT/HIGH_PCT` dari `getRuntimeThresholds()`. **Nuansa (jawaban pertanyaan audit #3):** rule engine `f_resid_warn/high` BUTUH `nominalLossSurplus < 0` (direction LOSS, rule-evaluation.ts L114-115) + rasio per-record basis QTY; lensa outlet memakai share nominal AGREGAT ABS tanpa gate arah (inheren outlet-grain — satu outlet mengagregasi dua arah). Paritas penuh di NILAI threshold (klaim master L406: "mustahil beda versi" — benar untuk nilai), bentuk predikat tidak identik — tercatat, bukan bug. |
| 4 | \|deviasi\|/penjualan per outlet | — (dihapus) | **MISMATCH (absent)** | **MATCH** | Shaping L536-552: `devPerSalesPct = sales>0 ? absNom/sales*100 : 0` — pembagi nol di-guard; filter `nominalSales>0 ∧ absNominalDev>0` (outlet tanpa penjualan dikecualikan); SQL L308 `MAX(nominalSales)` pembacaan aman single-week tanpa multiplikasi. |
| 5 | Kategori BAHAN vs PACKAGING via `Item.category` | — (dihapus) | **MISMATCH (absent)** | **MATCH** | CTE `cat_agg` L322-334: `JOIN "Item" i`, `COALESCE(NULLIF(i."category",''),'TANPA KATEGORI')`, GROUP BY 1; split absNominalDev/absQtyDev/residual/waste/susut/trial + sharePct (shaping L596-610). |
| 6 | Transfer antar outlet: item×area loss↔surplus serentak + outlet terbesar per sisi | — (dihapus) | **MISMATCH (absent)** | **MATCH** | `transfer_agg` L335-343 (GROUP BY itemId,area,direction WHERE direction IN ('LOSS','SURPLUS')) + `transfer_top` L344-354 (ROW_NUMBER PARTITION item,area,direction ORDER BY qtyDevAbs DESC → outlet terbesar per sisi); shaping L665-689: pairing key (itemId,area) loss↔surplus, `matchQty/matchNominal = min(loss,surplus)`. |
| 7 | Ketidakcocokan antar-area: pairing loss area A ↔ surplus area B dari baris `transfer_agg` SAMA (nol scan tambahan) | — (dihapus) | **MISMATCH (absent)** | **MATCH** | Shaping L695-727: pairing silang `l.area ≠ s.area` dari baris lens 'transfer' YANG SAMA (lossByItem/surplusByItem dibangun dari baris itu, L626-638) — nol scan/round-trip tambahan; kandidat penyelidikan bukan partisi; sort deterministik matchNominal→matchQty→itemId→area, slice topN; ringkasan `crossAreaSignalCount/Total`. |
| 8 | KRONIS ≥75% minggu / SPIKE minggu terburuk ≥60% (month-grain) | — (dihapus) | **MISMATCH (absent)** | **MATCH** | chronic-outlets.ts L96-99 konstanta `CHRONIC_MIN_WEEKS=3, CHRONIC_MIN_SHARE=0.75, SPIKE_MIN_WEEKS=2, SPIKE_SHARE=0.6` + `classifyChronic` L187-194: `nDevWeeks >= Math.ceil(0.75*nWeeks)` (operator ≥ benar untuk minggu integer: 4 mgg→≥3, 5 mgg→≥4=ceil(3.75)) dan `maxWeekSharePct >= 60` (≥). "Minggu terburuk" = argmax `SUM(ABS(nominalDeviasi))` per outlet×minggu via `ranked` ROW_NUMBER ORDER BY absNom DESC (L130-140). Min-minggu (≥3 kronis / ≥2 spike) terdokumentasi di AUDIT-REPORT §277 + header file. |
| 9 | Share \|qtyDeviasi\| berakhir 0/5 vs baseline periode (3 kolom FILTER di scan sama) | — (dihapus) | **MISMATCH (absent)** | **MATCH** | L314-318 (outlet_agg) + L374-378 (totals/baseline): `(ROUND(qtyDevAbs*10)::int % 5)=0` (menangkap akhiran .0/.5 + digit akhir 5/0) dan `% 10=0` (ketat "berakhir 0") — "3 kolom FILTER" = `nDev`/`nRound5`/`nRound10` di kedua CTE, scan sama, nol round-trip tambahan; shaping L558-593: ROUND_MIN_SAMPLE=3, Δ di atas baseline di-sort descending. |
| 10 | Momentum: rata \|dev\| paruh kedua vs pertama (lensa 'week' di query chronic sama) | — (dihapus) | **MISMATCH (absent)** | **MATCH** | Chronic L163-168 lensa 'week' (baris outlet×minggu, weekNo via `CAST(SUBSTRING(weekLabel FROM '[0-9]+') AS int)`) + `momentumOf` L218-234: sort weekNo, `split=floor(n/2)` → 4 minggu = 2\|2, 5 minggu = 2\|3 (index split, gap kalender diabaikan — terdokumentasi); band ±25% = STABLE; paruh-pertama-nol + paruh-kedua-ada → 'WORSE' dengan pct null. |
| 11 | Drilldown 4 tabel item-grain klik → modal `ItemDeepDive` reuse (a11y `clickableRowProps`) | — (dihapus) | **MISMATCH (absent)** | **MATCH** | Compliance.tsx: `clickableRowProps` diimport L50; 4 situs klik L292 (Kepatuhan Toleransi per Item), L351 (Prioritas Penetapan Toleransi), L565 (Indikasi Transfer Antar Outlet), L624 (Ketidakcocokan Antar-Area) → `setDeepDiveItem({itemName, outletCode:null})` → modal ItemDeepDive existing (nol endpoint baru). Tabel outlet-grain (residual L376 / penjualan L429 / kronis L659 / angka bulat L746) memang TIDAK klik-able — sesuai catatan master L406. |
| — | Struktur SQL compliance: CTE base direferensikan 6 agregat (tol_item, outlet_agg, cat_agg, transfer_agg, transfer_top ROW_NUMBER, totals) → dimaterialisasi SEKALI → UNION ALL (lens, to_jsonb(row)) | — (dihapus) | **MISMATCH (absent)** | **MATCH** | compliance.ts L255-408: `WITH base AS (...ABS/COALESCE per-record...)` direferensikan tepat 6 CTE bernama persis seperti master → multi-reference CTE tidak di-inlined PG → 1 scan fisik; hasil 6 branch `SELECT '<lens>' AS lens, to_jsonb(row)` UNION ALL = 1 round-trip; to_jsonb menormalkan COUNT bigint → angka JSON. Tidak ada scan berulang per lensa — lensa turunan (prioritas, antar-area, angka-bulat, momentum) dihitung di lapisan shaping JS dari baris/ kolom hasil yang sama. |
| — | Struktur SQL chronic: CTE wk (SUM per outlet×minggu) direferensikan 3 CTE + lensa 'week' | — (dihapus) | **MISMATCH (absent)** | **MATCH** | chronic-outlets.ts L119-169: `wk` direferensikan `ranked` + `top_week` + `outlet_agg` (3 CTE) + branch UNION lensa 'week' (referensi ke-4) → materialized SEKALI, 1 scan bulan, 1 round-trip. |
| — | Route: cache DB 5 mnt (SWR), rate-limit 30/mnt, maxDuration 60; chronic month-grain TANPA week (strict zod) | — (dihapus) | **MISMATCH (absent)** | **MATCH** | api/compliance/route.ts: `COMPLIANCE_CACHE_TTL=5*60*1000` + `withCacheAndDedup`, `rateLimit('compliance:'+ip, 30, 60_000)`, `maxDuration=60`, zod strict month+week wajib; api/chronic-outlets/route.ts: schema month SAJA (L43-49, komentar "strict zod menolak param liar — month-grain"), cache key tanpa week (L130-139) → reusable antar minggu. |

## FIX

**TIDAK ADA perubahan kode.** Alasan: satu-satunya "mismatch" adalah ABSENNYA seluruh fitur — dan absen itu adalah **deviasi disengaja + terdokumentasi + lebih baru** (kategori DOC-STALE per ATURAN FIX). Mengembalikan 2.268 LOC hasil penghapusan atas permintaan user bukan "diff minimal" dan akan membalikkan keputusan H-2b yang telah dibangun 63 commit di atasnya. File domain fix (`compliance.ts`, `chronic-outlets.ts`, `api/compliance/**`, `api/chronic-outlets/**`, `ComplianceTab/**`) semuanya tidak ada di working tree → tidak ada yang bisa/boleh diubah.

## DOC-STALE + BUKTI (rekomendasi untuk koordinator — dokumen di luar domain saya, READ-ONLY)

**Bukti inti (3 lapis):**
1. **Commit `b37cc40`** "refactor(ui): hapus tab Kepatuhan + endpoint /api/compliance & /api/chronic-outlets" (Sep 11 00:30, 63 commit sebelum HEAD): −2.268 LOC, sinkronisasi invalidasi/shortcut/routes-list, "nol konsumen/test lain (terverifikasi grep + build output)".
2. **Komentar kode hidup:** `src/lib/aggregation-cache/invalidate.ts:100` — "H-2b: removed the two Kepatuhan-tab routes (tab deleted with its APIs)." + `src/components/dashboard/ExportDialog.tsx:17-18` — "...removed **per user request** (same removal series as Loss-to-Sales / **Kepatuhan**)".
3. **Kontradiksi internal MASTER_CONTEXT:** baris 135 MENCATAT penghapusan ("(Earlier removals: ... H-2b Kepatuhan-tab deletion)") sementara baris 14, 103-104, 141-142, 189, 203, 383-406, 521, 549-550, 569-570, 595 masih mendeskripsikan fitur secara utuh. Section 383-406 = spesifikasi dari commit `9cf232f` yang lebih LAMA dari penghapusan.

**Spot dokumen stale lain (semua masih mendeskripsikan PAKET E):**
- `MASTER_CONTEXT.md`: L14 (header "Last updated... PAKET E"), L103-104 (tabel cache route), L141-142 (daftar API), L189+L203 (daftar komponen), L383-406 (section penuh — objek audit ini), L521 (hitungan komponen), L549-550+L569-570+L595 (file tree).
- `AUDIT-REPORT.md`: §266-282 (FEAT-COMPLIANCE-1..11) + §477 (ringkasan PAKET E).
- `PRD_RETROAKTIF.md`: L31, L237, L772-773, L885.
- `ARCHITECTURE.md`: L225-226, L301, L316-319, L388.
- `CONVENTIONS.md`: L149, L161, L811.
- Catatan numerik: "routes 27 → 35" (L14) kini ±33; "cached routes 20" tidak memuat compliance/chronic-outlets lagi (list L418 sudah benar pasca-H-2b).

**Kesimpalan perhitungan (pertanyaan user):** implementasi PAKET E yang pernah hidup **SESUAI master context untuk seluruh 11 lensa + struktur SQL kedua query** (11/11 MATCH + 2/2 struktur MATCH; 2 nuansa minor terdokumentasi di baris lensa 1 & 3 tabel di atas — keduanya soal bentuk populasi/predikat, bukan nilai threshold). Anehnya bukan perhitungannya — melainkan dokumen master yang belum dibersihkan setelah fitur dihapus atas permintaan user.

## VERIFIKASI PASCA-AUDIT

- `bunx tsc --noEmit --incremental false` → **exit 0, 0 error** (domain 0 trivial — file domain tidak ada; seluruh repo juga 0 → tidak ada referensi mati ke modul terhapus).
- `bun run lint` → **0 error**, 379 warning (semua pre-existing di tests/; identik baseline worklog BUG-HUNT-2 "379 warning"). Domain error = 0.
- Test compliance: `rg -l -i "compliance|chronic" tests/` → **tidak ada file test** (commit penghapusan memverifikasi "nol konsumen/test lain") → tidak ada yang perlu dijalankan. Test suite terkait domain lain (rule-evaluation.test.ts dll.) tidak disentuh — domain agent lain.
- Tidak menjalankan DB/dev/build; tidak commit/push; tidak menulis worklog.md.

Stage Summary:
- TEMUAN UTAMA: seluruh PAKET E (tab Kepatuhan ke-6, 11 lensa, 2 query, 2 route, 2 komponen FE — 2.268 LOC) TIDAK ADA di HEAD; dihapus sengaja atas permintaan user di commit `b37cc40` (H-2b) dan terdokumentasi di komentar kode + MASTER_CONTEXT:135. Spec master L383-406 (dan ~25 spot dokumen pendamping di MASTER_CONTEXT/AUDIT-REPORT/PRD/ARCHITECTURE/CONVENTIONS) = **DOC-STALE**.
- Audit forensik pra-penghapusan (git `b37cc40^`): seluruh 11 lensa + struktur SQL kedua query MATCH terhadap kutipan master — termasuk paritas threshold penuh (route inject `getRuntimeThresholds()`: STD_DEVIASI_BOM_PCT/RESIDUAL_LOSS_WARN_PCT/RESIDUAL_LOSS_HIGH_PCT; 0 hardcode), operator ≥ benar di lensa 8 (ceil(0.75×n)+>=, >=60), mod-5/mod-10 lensa 9 dengan 3 kolom FILTER di scan sama, index-split paruh lensa 10 (4→2\|2, 5→2\|3), guard pembagi nol lensa 4, dan 4 tabel item-grain→ItemDeepDive lensa 11. Dua nuansa non-bug dicatat (populasi absNominalDeviasi>0 tidak direplikasi; paritas lensa 3 berlaku untuk nilai threshold, bukan bentuk predikat arah-LOSS per-record).
- FIX: TIDAK ADA (DOC-STALE → per aturan, kode tidak diubah; mengembalikan fitur bukan diff minimal dan melawan keputusan user yang tercatat).
- Verifikasi: tsc 0 error, lint 0 error (379 warning pre-existing), 0 test compliance ada. Rekomendasi tindak lanjut: task pembersihan dokumen (di luar domain saya) untuk menghapus/menganotasi spot DOC-STALE yang terdaftar di atas.
