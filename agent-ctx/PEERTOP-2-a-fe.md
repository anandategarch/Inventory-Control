Task ID: PEERTOP-2-a
Agent: general-purpose (frontend)
Task: Peer tab — Top Items Across Peers card + expandable per-peer top item rows

Work Log:
- Read worklog.md (last 150 lines) + the full PEERTOP-1 contract before writing: `src/lib/queries/outlets/peer-top-items.ts` (result types + server sort order), `src/app/api/peer-comparison/top-items/route.ts` (response envelope), `use-peer-queries.ts`, `items-table.tsx` (loading/error/empty state pattern), `peer-table-card.tsx`, `index.tsx`, `peer-comparison/types.ts`, `helpers.ts`. Also checked `@/lib/format` (`fmtPct(v, withSign, digits)`), `@/lib/a11y` (`clickableRowProps`), and `InfoTooltip` usage.
- `src/components/dashboard/peer-comparison/types.ts` — added client-safe `PeerTopItemsResponse` interface mirroring the backend result shape verbatim (items union rows + perPeer; NO import from `@/lib/queries/**` — that pulls Prisma; same policy as `ItemComparisonResponse`). Documented server sort order, "Rata-Rata Absolute" basis, blind-spot null semantics, and the missing-perPeer-entry case.
- `src/components/dashboard/PeerComparison/use-peer-queries.ts` — added query #5 `topItems`: queryKey `['peer-comparison', 'top-items', activeOutlet, monthLabel, currentWeek, kelompok]`; queryFn builds URLSearchParams (outletCode, month, week if set, mode='week', topN='5', limit=String(peerLimit) — peerLimit const = 50, so the peer band is IDENTICAL to the main query's, perPeer maps 1:1 onto Peer Table rows, kelompok when != 'all'); fetch `/api/peer-comparison/top-items?${p}` with the sibling content-type guard + `if (!res.ok)` throw-JSON-error-body pattern; typed `Promise<PeerTopItemsResponse>`; `enabled: Boolean(activeOutlet && monthLabel && currentWeek)`, `staleTime: 5*60_000`, `gcTime: 10*60_000`. Hook returns `topItemsData, topItemsLoading, topItemsError` (with comment).
  - LINT FIX during verification: initially used `activeOutlet!`/`monthLabel!` like the items query → +2 `no-non-null-assertion` warnings (379 vs 377 baseline). Switched to the benchmark-opportunity query's guard convention (`if (!activeOutlet || !monthLabel) throw new Error(...)` — documented in-repo as "keeps TS strict happy without adding a new lint warning") → back to exactly 377 warnings, zero new.
- NEW `src/components/dashboard/peer-comparison/top-items-card.tsx` — memo'd `TopItemsAcrossPeers` ({ data, isLoading, error, totalPeers, topN }). Card shell identical to items-table.tsx (Layers icon, "Top Items Across Peers"), subtitle "Top {topN} item di tiap resto setara — masalah bersama vs khusus resto kamu." + InfoTooltip with the agreed explanation. 4 states: amber Loader2 / "Pilih outlet untuk melihat top item peer" / red error / "Tidak ada item deviasi pada periode ini." Table with 6 columns: Item (truncate max-w-[220px] + title), "Top di" ({peerTopCount}/{totalPeers}, title = peer names via perPeer code→name Map, falls back to raw code for peers w/o deviation records), Rank di Target (null → — w/ title "Tidak ada di resto kamu"; rank<=topN → #rank; else muted "> topN" w/ title), Nominal (Target) (fmtIDR; font-semibold when rank<=topN, muted otherwise), "Rata-Rata Absolute" (fmtIDR(peerAvgAbsNominal), — when peerTopCount=0 — master-context ABSOLUTE terminology), Dir (direction letter, LOSS red / SURPLUS emerald / muted). Table wrapped in max-h-[500px] overflow-y-auto; NO client-side sorting (server pre-sorted); footer 💡 note about systemic items + blind spots.
- `src/components/dashboard/PeerComparison/peer-table-card.tsx` — new props `topItemsData`/`topItemsLoading`; `useState<string|null>(expanded)` single-open accordion; memoized code→perPeer Map. Chevron toggle in each outlet row's FIRST cell (before the outlet name): native `<button type="button">` h-9 w-9 (≥36px touch target) rounded-md hover:bg-muted, `onClick` with stopPropagation+preventDefault (preserves the row's clickableRowProps retarget), `onKeyDown` guard for Enter/Space (otherwise the keydown bubbles to the row's clickableRowProps onKeyDown and RETARGETS instead of expanding — real keyboard UX bug), `aria-expanded`, `aria-label="Lihat top item {outletName}"`, ChevronDown when expanded / ChevronRight when collapsed (h-3.5 w-3.5 text-muted-foreground). Rows wrapped in `<Fragment key={outletCode}>`; after each row, when expanded, an expansion `<TableRow>` with one `<TableCell colSpan={4 + columns.length + 2}>` (= 17) renders the new memo'd `PeerTopItemsExpansion`: missing entry → "Memuat top item…" (loading) / "Tidak ada item deviasi pada periode ini." (muted); else bordered rounded bg-muted/10 container with title "Top item di {outletName}" + "{n} item", each item a compact row `#{i+1}` muted tabular-nums → name (truncate+title) → fmtIDR(absNominal) font-mono tabular-nums → fmtPct(devBom, false) → direction letter colored (local `dirColor` helper, same convention as the Dir column). Footer hint appended: "Klik ▸/▾ di baris untuk lihat top item resto tersebut." ALL existing behavior intact (peer-average row, target highlighting, colorCell, AnomalyFlags, sticky header/left column, retarget).
- `src/components/dashboard/PeerComparison/index.tsx` — destructured `topItemsData, topItemsLoading, topItemsError` from `usePeerQueries`; render order now header card → analysis grid → PeerTableCard (with the two new props) → NEW TopItemsAcrossPeers (totalPeers={otherPeers.length}, topN={5}) → ItemLevelComparison → TrendChartCard → CorrelationInsightCard (peer-centric block grouped together); added `PeerTopItemsResponse` to the shared-types re-export list.
- VERIFICATION (static — DB unreachable in sandbox, per rules): `bunx tsc --noEmit --incremental false` → EXIT 0; `bun run lint` → **0 errors, 377 warnings** (= pre-existing baseline, zero new). Confirmed via `git status`/mtimes that ONLY the 5 assigned files were touched by me (backend PEERTOP-1 files + export-report changes in the working tree belong to sibling agents, untouched by me). No tests written/run, no build, no dev server, no commit/push, worklog.md untouched.

Stage Summary:
- Files changed (5, exactly as scoped): `src/components/dashboard/peer-comparison/types.ts` (+PeerTopItemsResponse client-safe type), `src/components/dashboard/PeerComparison/use-peer-queries.ts` (+topItems query, limit=peerLimit(50) for identical peer band), `src/components/dashboard/peer-comparison/top-items-card.tsx` (NEW — Top Items Across Peers union card), `src/components/dashboard/PeerComparison/peer-table-card.tsx` (expandable ▸/▾ per-peer top-N rows), `src/components/dashboard/PeerComparison/index.tsx` (wiring + render order).
- Key decisions: (1) guard-style null checks instead of `!` assertions in the new queryFn to keep the lint warning count at baseline 377 (repo's benchmark-opportunity convention); (2) chevron button also stops keydown propagation so keyboard Enter/Space expands instead of triggering the row's retarget handler; (3) code→name Map falls back to raw outletCode for peers with zero deviation records (they have no perPeer entry); (4) no client-side sorting — server sort order (peerTopCount desc → target absNominal desc → peerMaxAbsNominal desc → name) preserved; (5) expansion row is a normal colSpan row across all 17 columns (sticky styling unaffected).
- tsc: 0 errors (exit 0). lint: 0 errors / 377 warnings (baseline preserved).
