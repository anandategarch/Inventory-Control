---
Task ID: BUG-G
Agent: full-stack-developer
Task: Bug hunt + fix (BUKAN refactor gaya) di domain SPLIT-G (TopGrowthCard/, RestoAnalysis/, PeerComparison/, narrative/OutletPriorityPanel/) + SPLIT-C (FileUploadDialog/pipeline/, FilterBar/) — audit diferensial terhadap baseline pre-split `git show 28d7930:<path>`; perbaiki HANYA bug nyata, minimal diff.

Work Log:
- Konteks: `tail -n 120 worklog.md` (SPLIT-2 batch: 7e0e0d4 SPLIT-G tanpa audit diferensial; b8fb813 SPLIT-C dengan laporan mekanis yang di-double-check di sini) + `agent-ctx/SPLIT-G-split.md` + `agent-ctx/SPLIT-C-split.md`.
- Baseline pre-split diekstrak utuh ke /tmp/bugg-baseline/ via `git show 28d7930:<path>`: TopGrowthCard.tsx 555, RestoAnalysis.tsx 551, PeerComparison.tsx 525, narrative/OutletPriorityPanel.tsx 527, FilterBar.tsx 617, FileUploadDialog/use-upload-pipeline.ts 662, FileUploadDialog/index.tsx 218.
- AUDIT DIFERENSIAL — dibaca penuh baseline vs SEMUA 32 file modul baru, baris-per-baris, dengan fokus kelas bug yang diburu:
  a. Hook custom baru:
     - `use-peer-queries.ts` (256): urutan 4 useQuery (main → items → trend → opportunity) SAMA; queryKey/queryFn (URL params + kelompok gating `kelompok && kelompok !== 'all'`)/enabled/staleTime 5m/gcTime 10m/keepPreviousData identik; 5 memo derivasi (peers/targetRow/otherPeers/peerCodes/peerCodesKey) + peerAverages (11 field) + efficiencyScore/gapRows/scatterPoints/rankData — deps array identik dengan baseline; tidak ada stale closure (queryFn menangkap param render terakhir, sama seperti closure asli yang juga re-created per render).
     - `use-lens-data.ts` (151): urutan hook terjaga persis (useRecommendations → recommendations memo → totalDev → defaultCount → healthRanking → opp query `enabled: isPeluang && …` → chg query `enabled: isPerubahan && …` → changeOutlets filter DATA_KURANG); queryKey benchmark-opportunity & change-analysis identik; LENS-GATED enable identik → tidak ada request eager baru.
  b. Props tertukar (tipe sama, tsc tak menangkap) — semua pasangan rawan diperiksa satu per satu: GrowthRow `grainNoun`/`childNoun` (keduanya string) ✓ benar; OutletHeaderCard `isFetching`/`isLoading` (keduanya boolean) ✓ benar (`{isFetching && !isLoading}` spinner); PeerTableCard `peers`/`otherPeers` (keduanya PeerRow[]) ✓ benar; PeriodSelects `monthLabel`/`currentWeek`/`compareValue` (semua string) ✓ benar; OrgSelects `pic`/`area`/`kelompok`/`outletCode` + 4 setter-nya ✓ terpasang ke combobox yang benar; FilterActions `onOpenSettings`/`onOpenDataMgmt`/`onOpenPicMgmt` (semua () => void) ✓ terpasang ke tombol ikon yang benar (Settings/Database(=Data)/Users(=PIC)) dan dialog mount di index memakai state yang benar; CompactRowView `monthLabel`/`currentWeek`/`kelompok` → ChangeItemTable (`week={currentWeek}`) ✓.
  c. Early-return / empty-state: RestoAnalysis urutan 4 early-return (!activeOutlet → !month/!week → isLoading → error/!success) + kartu partial-profile (BUG-2-a #2) terjaga; TopGrowthCard 3 kondisi kosong (!topGrowth → !comparisonWeek → rows.length===0) terjaga; PeerComparison cabang tabel (mainLoading → !mainData&&!mainError (disabled-query FE-1) → mainError||!success → peers.length===0) terjaga; OutletPriorityPanel 7 cabang loading/error/kosong per lensa + expand/collapse (`Tampilkan {min(COMPACT_MAX,totalCount)-shown.length} lagi`) terjaga.
  d. Key/sort/format: `TopGrowthCard/format.ts` — formatDelta/formatDeltaSigned/formatQty/formatQtySigned (guard BUG-HUNT B6)/formatPeriodLabel byte-identik dengan baseline; sort `healthScore asc` (kondisi), `opportunityRp desc` (peluang), prioritas footer share-cap `Math.min(1, …)` identik; keys `r.name`/`r.rank`/`p.outletCode`/`c.name`/`r.key`/`${p.weekLabel}|${p.monthLabel}` identik.
  e. State ownership & initial value: grain 'outlet' + expanded null (TopGrowth); rankingTab 'financial' + selectedItem null (Resto); lens 'prioritas' + expanded false + expandedOutlet null (OutletPriority); 3 state dialog FilterBar — semua initial value & lokasi ownership identik; handleGrainChange/handleLensChange reset expanded identik.
- SPLIT-C pipeline (bagian yang TIDAK diverifikasi agent-nya, diverifikasi di sini manual):
  - `pipeline/abort.ts`: UPLOAD_CHUNK_TIMEOUT_MS=120_000 / DETECT_TIMEOUT_MS=60_000 / IMPORT_TIMEOUT_MS=300_000 — nilai SAMA dengan baseline; `fetchWithTimeout` (add controller → setTimeout abort → finally clearTimeout+delete dari Set) identik; `cancel()` urutan: set flag → abort semua → clear Set → unlock (setUploading(false)+setImporting(false) via onUnlock pada posisi setter asli) identik; identitas `cancel` tetap stabil (useCallback [onUnlock], onUnlock=unlockBusy useCallback []) ≈ baseline useCallback [].
  - `upload-detect-stage.ts`: SHA-256 → chunk 4MB → worker pool 3-concurrency dengan shared cursor + last-chunk ditahan (server verifikasi jumlah+ukuran) → confirm `uploaded` → detect → short-circuit semua-week-sudah-ada (DELETE temp + invalidate ['status'] + toast + setUploading(false)) → set detectData → berhenti di panel konfirmasi; catch: cancelRequestedRef quiet-return, AbortError → pesan timeout dengan phaseTimeoutMs dinamis (120s chunk vs 60s detect) — semua verbatim.
  - `import-stage.ts`: payload import-all (+ manualFileName saat manualMode), branch 504, per-week statusLog, DELETE cleanup, result assembly (totalSkipped=existingWeeks.length), invalidateAllData, toast sukses/gagal, `finally { setImporting(false) }` — verbatim.
  - `file-selection.ts`: dedup applySelectedFile — kedua handler baseline punya sekuens validasi+setState byte-identik (pesan error verbatim), jadi dedup tanpa perubahan perilaku; ini SATU-SATUNYA bukan byte-for-byte copy di SPLIT-C (dikonfirmasi ulang independen).
  - `use-upload-pipeline.ts` entry: deps object di-rebuild tiap render = parity dengan closure asli yang juga re-created tiap render → handler klik selalu melihat state render terakhir (file/renameMode/manualValidation/numberLocale). Barrel re-export 4 type publik → import path ConfirmPanel/ImportResultSummary/index.tsx tak berubah.
- FilterBar/: `use-filter-bar-state.ts` — selector useShallow 17 field verbatim, effect BUG-FE-9 dengan deps array identik, derivasi weeks (lookup via months key), memo outlets [status?.outlets, area, pic, kelompok], allComparePeriods (eksklusi bulan kini + sortKey padStart), compareValue, activeFilterCount 5 filter, handler atomik setPeriod — identik. `use-ingest.ts` (AbortController 120s + content-type guard + invalidateAllData + auto-dismiss 8s) identik. PeriodSelects (hover-prefetch dgn kelompok BUG-FE-1 + compare resolved PERF-1; usePrefetchAnalysis tetap 1 call), OrgSelects, FilterActions (CustomEvent open-drive/open-upload dialog), lazy-dialogs (3 dynamic ssr:false), index (urutan state + struktur render) — identik. Normalisasi `status?.kelompokOptions?.length || 0` ≡ `kelompokOptions.length` (kelompokOptions = `status?.kelompokOptions || []`) — ekuivalen.
- AUDIT MEKANIS tambahan (python, multiset token non-komentar/non-import baseline vs gabungan modul baru): OLD-ONLY untuk RestoAnalysis & PeerComparison = NONE (tidak ada token logika baseline yang hilang); delta lain semuanya plumbing yang terjelaskan (props/interface/export/Dispatch, dedup validasi file, rename tipe Period→ComparePeriodOption, helper periodSelects()/orgSelects() → komponen).
- Bug-hunt umum domain (pre-existing, unchanged sejak 28d7930): dialog Settings/Pic/DataManagement/DriveImport + SearchableComboBox + upload-utils di-scan — tidak ada crash-level/null-safety baru; temuan berupa warning pre-existing (lihat TIDAK DIPERBAIKI).

DIPERBAIKI (1 perbaikan, PRE-EXISTING, kosmetik — di-sanction task):
- `src/components/filters/FileUploadDialog/index.tsx` — warning `'open' is defined but never used` (no-unused-vars base rule, flag nama param pada TYPE `(open: boolean) => void`). Diverifikasi BUKAN bug nyata: prop `open` DIPAKAI (`<Dialog open={open} …>`); onOpenChange DIPAKAI (diteruskan ke useUploadPipeline). Ini murni nama param posisi-tipe yang di-flag rule TS-blind. Fix rapi 1 perubahan mengikuti konvensi repo di file SPLIT (growth-row/compact-row/peer-table-card: `Dispatch<T>` tanpa nama param):
  ```diff
  +import type { Dispatch } from 'react';
   interface FileUploadDialogProps {
     open: boolean;
  -  onOpenChange: (open: boolean) => void;
  +  // (Dispatch<boolean> = (value: boolean) => void, spelled without a param
  +  //  name — the repo's base no-unused-vars rule flags type-position params.)
  +  onOpenChange: Dispatch<boolean>;
   }
  ```
  Assignability dicek: caller page.tsx mengirim `setUploadDialogOpen` (Dispatch<SetStateAction<boolean>>) — kontravarian parameter, assignable ✓ (tsc 0 error).

TIDAK diperbaiki + alasan:
1. Warning 'open' serupa di `use-upload-pipeline.ts:50` & `DriveImportDialog.tsx:39`, `'v'` di Settings/Data/PicManagementDialog, `any` di upload-detect-stage/use-ingest, `value`/`className` unused di SearchableComboBox — semua PRE-EXISTING (ada identik di 28d7930), bukan bug perilaku (prop/state yang bersangkutan dipakai atau memang API prop), dan task hanya men-sanction pembersihan 1 baris untuk warning yang disebut; memperluasnya = berlebihan.
2. `PicManagementDialog.tsx:97` `const outlets = status?.outlets ?? []` → identitas baru tiap render → 2 warning react-hooks/exhaustive-deps (memo deps berubah tiap render) — PRE-EXISTING perf smell, bukan correctness; fix = bungkus useMemo (refactor).
3. `use-ingest.ts` finally `setTimeout(() => setIngestMsg(null), 8000)` — dua ingest beruntun <8s bisa saling menghapus pesan (timer pertama meng-clear pesan kedua) — PRE-EXISTING edge minor UX; fix butuh timer-ref plumbing di luar mandat minimal-diff; setState-after-unmount = no-op React 18.
4. `StalePayloadEmptyState` re-arm `window.setTimeout(120_000)` — PRE-EXISTING; setState-after-unmount no-op React 18; tidak mengubah perilaku yang dirasakan user.
5. Komponen rekan (tabs/**, TopItems, InsightsPanel, HistoricalZScoreCard, BomCorrelationCard, PriceEffectCard, Charts/, resto-analysis/, peer-comparison/, peer-computation) — READ-ONLY sesuai batasan domain (tidak tersentuh SPLIT-G; konsumen tipe/helper diverifikasi hanya lewat tsc).

HASIL VERIFIKASI (pasca-fix; agent lain paralel — error luar domain diabaikan):
- `bunx tsc --noEmit --incremental false` → **0 error** seluruh repo (exit 0).
- `bun run lint 2>&1 | tail -40` → **0 error, 381 warning** repo-wide (baseline sesi SPLIT-2 = 382; turun tepat 1 = warning 'open' yang dihapus; 0 warning baru). Domain check `bunx eslint src/components/filters/ + 4 folder SPLIT-G` → 0 error, 42 warning (semua pre-existing: any×7, non-null assertion×4, unused vars/param-name tipe sisanya).
- `rg -l "TopGrowth|RestoAnalysis|PeerComparison|OutletPriority|FilterBar|FileUpload|upload" tests/` → 4 file terkait: `bunx vitest run tests/queries/top-growth.test.ts tests/lib/query-invalidation.test.ts tests/hooks/use-toast.test.ts tests/lib/payload-schema.test.ts` → **24/24 PASS** (top-growth 6, payload-schema 11, query-invalidation 3, use-toast 4). Full suite `bunx vitest run` → **31 file / 512/512 PASS**.
- `git status` → file domain saya yang berubah hanya 1 (`FileUploadDialog/index.tsx`, +4/−1) + laporan ini; tidak ada git add/commit/push. Catatan: 3 file di luar domain (`api/analysis/services/validate-and-resolve.ts`, `api/peer-comparison/items/route.ts`, `hooks/useRecommendations.ts`) muncul dimodifikasi di working tree oleh agent paralel SELAMA sesi ini — tidak saya sentuh sesuai batasan; tsc tetap 0 error pada akhir sesi.

Stage Summary:
- KESIMPULAN UTAMA: **tidak ditemukan bug nyata yang diperkenalkan SPLIT-G (7e0e0d4) maupun SPLIT-C (b8fb813)** — keduanya terbukti pure code motion di domain saya. Audit diferensial penuh 6 baseline file (3.437 baris) vs 32 modul baru (±3.100 baris): semua queryKey/enabled/timeout/deps-memo/urutan-hook/early-return/key/sort/format/props-wiring/initial-state identik; klaim verifikasi mekanis SPLIT-C (sekuens setState/fetch/toast) TIDAK terpatahkan oleh audit independen ini, termasuk area yang tidak diverifikasi agent-nya (abort/timeout 120s-60s-300s, concurrency chunk 3 + last-chunk held, import-stage, dedup applySelectedFile).
- 1 perbaikan: warning 'open' pre-existing di FileUploadDialog/index.tsx dihapus rapi (Dispatch<boolean>, konvensi repo) — dipastikan bukan bug nyata sebelum dihapus.
- Deliverable cakupan diferensial per komponen (baseline → modul; hasil = IDENTIK kecuali plumbing):

| Komponen | Baseline → Modul | Coverage diferensial | Temuan |
|---|---|---|---|
| TopGrowthCard | 555 → 6 file (format/empty-states/period-legend/contributor/growth-row/index) | 100% baris logika; format.ts fungsi-per-fungsi; props string Kembar grainNoun/childNoun dicek | BERSIH |
| RestoAnalysis | 551 → 5 file (state-cards/outlet-header-card/profile-cards/bahan-analysis-card/index) | 100%; 4 early-return; ring health; 6 kartu profil; tabel 14 kolom; useQuery+sharedReco | BERSIH |
| PeerComparison | 525 → 4 file (use-peer-queries/peer-table-card/no-outlet-card/index) | 100%; 4 query + 9 memo deps; peers/otherPeers & boolean Kembar dicek | BERSIH |
| OutletPriorityPanel | 527 → 5 file (lens-model/use-lens-data/build-display-rows/compact-row/index) | 100%; urutan hook; lens-gated enabled; mini-bar scale; ChangeItemTable wiring | BERSIH |
| use-upload-pipeline | 662 → entry 211 + pipeline/ 5 file | 100%; abort/timeout/cancel; chunk pool; import-stage; dedup file-selection (dik-onfirmasi ekuivalen) | BERSIH |
| FilterBar | 617 → 7 file | 100%; effect BUG-FE-9 deps; handler atomik; prefetch params; 4 combobox wiring; kelompokOptions normalisasi ekuivalen | BERSIH |

- Gates: tsc 0 err · eslint 0 err (381 warning = 382 baseline − 1, tanpa warning baru) · vitest 512/512 (+ 24/24 test terkait domain). Laporan ini TIDAK menulis worklog.md sesuai batasan.
